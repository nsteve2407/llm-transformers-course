# Modules 2-3 (CNN Refresher, RNN+LSTM) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete Part 1 of the course by building out Module 2 (CNN Refresher) and Module 3 (RNNs + LSTMs) — content, reading list, quiz, and coding exercise notebooks — following the exact structural pattern established by Module 1.

**Architecture:** Same as the existing site: Jekyll content pages under `modules/<slug>/`, quiz data under `_data/quizzes/<slug_with_underscores>.yml`, notebooks under `notebooks/<slug>/`. All shared infrastructure (quiz engine, chat widget, Worker, CI) already exists and needs no changes — only content is added.

**Tech Stack:** Jekyll/Liquid/YAML (content), PyTorch/Jupyter (notebooks), same toolchain as the existing repo (Ruby 3.2.11 via rbenv, Node.js 20 via the `~/.local/bin` symlinks already set up on this machine).

## Global Constraints

- Every reading-list URL must be copied EXACTLY from `docs/superpowers/specs/2026-07-31-llm-transformers-course-design.md` §4 (Module 2 at line 121, Module 3 at line 160) — do not invent, alter, or "improve" any URL.
- Quiz include syntax must use **dot notation** (`site.data.quizzes.NN_slug`), never bracket notation — bracket notation is a hard Jekyll build failure (discovered and documented during Module 1). See `modules/01-dnn-refresher/index.md` for the working example.
- New module pages must set `has_children: true` if they have a reading.md child page (Module 1 originally shipped with this wrong — `has_children: false` — which made the whole module invisible in the sidebar nav; this was fixed in the final review of Plan 1. Don't repeat that mistake here.) `reading.md`'s `parent:` must exactly match the module page's `title:`.
- Module pages nest under the existing `modules/index.md` (`title: Modules`) via `parent: Modules`.
- The chat widget include (`{% include chat-widget.html slug=page.slug title=page.title %}`) can be added directly in each module's `index.md` from the start — unlike Module 1, `_includes/chat-widget.html` already exists, so there's no ordering hazard to work around.
- Quiz data file naming: directory slug is hyphenated (`02-cnn-refresher`), but the `_data/quizzes/` YAML filename and its Liquid dot-notation key must be **underscored** (`02_cnn_refresher`) — Jekyll data keys can't contain hyphens for dot-notation lookup. See README's "Quiz data convention" section.
- Quiz question schema (from `assets/js/quiz-logic.mjs`, already built and tested): `{type: "mcq", question, choices: string[], answer_index: number, explanation}` or `{type: "short", question, answer_keywords: string[], model_answer, explanation}`. `answer_index` must be a valid index into `choices`. `answer_keywords` must be non-empty.
- Notebook convention (established in Module 1, `notebooks/01-dnn-refresher/`): paired `exercise_starter.ipynb` / `exercise_solution.ipynb`, nbformat 4 JSON, opening markdown cell with title + "Open in Colab" badge (`https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/<slug>/<file>.ipynb`) + module page link + objective summary. A `SMOKE_TEST` environment variable (checked via `os.environ.get("SMOKE_TEST") == "1"`) swaps any real dataset download for small synthetic random tensors and reduces epochs to 1, so the notebook can be verified quickly and without network access; unset, it uses the real dataset/full training for actual Colab use. The starter notebook must genuinely fail with `NotImplementedError` when smoke-tested (proving its TODO stubs sit in the executed path), and the solution notebook must smoke-test clean.
- Every module's `_data/quizzes/<slug>.yml` should have 8-10 questions, a mix of `mcq` and `short` types (roughly 70/30 mcq/short, matching Module 1's 7/3 split — not a hard rule, use judgment), covering the module's full "Quiz topics" list from the spec.

---

## File Structure

```
modules/
  02-cnn-refresher/
    index.md          # content page: subtopics, reading pointer, exercise summary, quiz include, chat widget include
    reading.md         # required + optional reading + lecture references
  03-rnn-lstm/
    index.md
    reading.md
_data/quizzes/
  02_cnn_refresher.yml   # 8-10 questions
  03_rnn_lstm.yml        # 8-10 questions
notebooks/
  02-cnn-refresher/
    exercise_starter.ipynb
    exercise_solution.ipynb
  03-rnn-lstm/
    exercise_starter.ipynb
    exercise_solution.ipynb
```

---

### Task 1: Module 2 content pages (index.md + reading.md)

**Files:**
- Create: `modules/02-cnn-refresher/index.md`
- Create: `modules/02-cnn-refresher/reading.md`

**Interfaces:**
- Produces: the page that Task 2 (quiz) wires into via the pre-existing `{% include quiz.html slug=page.slug quiz=site.data.quizzes.02_cnn_refresher %}` line (write this include directly in this task — the quiz data file doesn't exist until Task 2, but Jekyll doesn't error on a currently-undefined `site.data` lookup, it just renders an empty quiz list, so the build stays green throughout).

- [ ] **Step 1: Write `modules/02-cnn-refresher/index.md`**

```markdown
---
title: "2. CNN Refresher"
parent: Modules
nav_order: 2
slug: 02-cnn-refresher
has_children: true
---

# Module 2: CNN Refresher

Convolutions, pooling, classic architectures, receptive fields, batchnorm in CNNs.

## Subtopics

- Output-dimension arithmetic from kernel/stride/padding/dilation; same vs. valid padding; depthwise/grouped convs
- Parameter sharing → translation equivariance vs. invariance
- 1x1 convolutions as per-pixel channel projections; bottleneck blocks, Inception modules
- Max vs. average pooling; strided convs as a pooling replacement
- Receptive field growth across stacked layers; why two 3x3 convs beat one 5x5
- LeNet-5: alternating conv/subsampling, tanh/sigmoid activations
- AlexNet: ReLU, dropout, local response norm, two-GPU split
- VGG: stacked 3x3 convs, uniform depth scaling
- The empirical "degradation problem" in deep plain CNNs (distinct from vanishing gradients)
- ResNet: identity skip connections F(x)+x, basic vs. bottleneck blocks
- BatchNorm in CNNs: per-channel stats across (N,H,W), conv→BN→ReLU convention
- Forward pointer to multi-scale/attention-based vision models

## Reading list

See [Module 2 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"Depth, gradients, and residuals on a CIFAR-10 subset"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/02-cnn-refresher/exercise_starter.ipynb){:target="_blank"}.

Implement `Conv2dManual` and validate against `F.conv2d`. Build "PlainNet" (no skips) and "ResNet" counterparts at 3 depths (8/20/44 layers), train on a CIFAR-10 subset. Plot training loss and first-layer gradient norms across all six — should reproduce the degradation effect in plain nets and healthy gradients in ResNets.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.02_cnn_refresher %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/02-cnn-refresher/reading.md`**

```markdown
---
title: "Reading List"
parent: "2. CNN Refresher"
nav_order: 1
---

# Module 2 Reading List

## Required

- LeCun, Bottou, Bengio, Haffner (1998). *Gradient-Based Learning Applied to Document Recognition.* <https://cs.nyu.edu/~yann/2010f-G22-2565-001/diglib/lecun-98.pdf>
- Krizhevsky, Sutskever, Hinton (2012). *ImageNet Classification with Deep CNNs (AlexNet).* <https://papers.nips.cc/paper/4824-imagenet-classification-with-deep-convolutional-neural-networks>
- Simonyan & Zisserman (2014). *Very Deep Convolutional Networks (VGG).* <https://arxiv.org/abs/1409.1556>
- He, Zhang, Ren, Sun (2015). *Deep Residual Learning (ResNet).* <https://arxiv.org/abs/1512.03385>
- Ioffe & Szegedy (2015). *Batch Normalization.* <https://arxiv.org/abs/1502.03167>

## Optional / further reading

- Dumoulin & Visin (2016). *A Guide to Convolution Arithmetic.* <https://arxiv.org/abs/1603.07285>
- Araujo, Norris, Sim (2019). *Computing Receptive Fields of CNNs.* Distill. <https://distill.pub/2019/computing-receptive-fields/>
- Olah, Mordvintsev, Schubert (2017). *Feature Visualization.* Distill. <https://distill.pub/2017/feature-visualization/>
- Stanford CS231n, *Convolutional Neural Networks.* <https://cs231n.github.io/convolutional-networks/>

## Lecture references

- Stanford CS231n course hub. <https://cs231n.github.io/>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build`
Expected: no errors; `_site/modules/02-cnn-refresher/index.html` and `_site/modules/02-cnn-refresher/reading.html` exist.

- [ ] **Step 4: Commit**

```bash
git add modules/02-cnn-refresher/index.md modules/02-cnn-refresher/reading.md
git commit -m "Add Module 2 (CNN Refresher) content and reading list"
```

---

### Task 2: Module 2 quiz data

**Files:**
- Create: `_data/quizzes/02_cnn_refresher.yml`

**Interfaces:**
- Consumes: the schema from `assets/js/quiz-logic.mjs` (already built, Task 4 of Plan 1) — `{type: mcq, question, choices, answer_index, explanation}` or `{type: short, question, answer_keywords, model_answer, explanation}`.
- Produces: `site.data.quizzes.02_cnn_refresher`, already referenced by `modules/02-cnn-refresher/index.md` (Task 1).

- [ ] **Step 1: Author 8-10 quiz questions**

Write `_data/quizzes/02_cnn_refresher.yml` covering these topics (from the design spec's Module 2 "Quiz topics" list — write one full question per topic, mixing `mcq` and `short` types, roughly 70/30 mcq/short):

1. Output-shape arithmetic from kernel/stride/padding/dilation given concrete numbers
2. Parameter sharing → translation equivariance vs. invariance (conceptual distinction)
3. 1x1 convolutions as per-pixel channel projections / their role in bottleneck blocks
4. Effective receptive field after stacking N k×k conv layers
5. Why two stacked 3x3 convs beat one 5x5 conv (parameter count vs. receptive field vs. nonlinearity)
6. Max pooling vs. average pooling — what each discards/preserves
7. AlexNet's specific innovations and what problem each solved (ReLU, dropout, LRN)
8. The empirical degradation problem in deep plain CNNs vs. vanishing gradients — these are different phenomena
9. Why F(x)+x (the residual/skip connection) eases gradient flow vs. a plain nonlinear stack
10. BatchNorm's normalization axes for a (N,C,H,W) feature map, and train vs. inference statistics

Use `_data/quizzes/01_dnn_refresher.yml` (already in the repo) as your concrete style reference: same YAML structure, same level of explanation detail (2-3 sentences per explanation, technically precise, teaching the "why" not just restating the answer), same `model_answer` style for short-answer questions. For MCQ questions, write 4 plausible choices where the distractors reflect genuine misconceptions (e.g. confusing degradation with vanishing gradients), not obviously-wrong filler.

- [ ] **Step 2: Verify the YAML parses and the page renders**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/02_cnn_refresher.yml').length"`
Expected: a number between 8 and 10.

Run a schema check (every mcq has valid `answer_index`, every short has non-empty `answer_keywords`):
```bash
ruby -ryaml -e '
q = YAML.load_file("_data/quizzes/02_cnn_refresher.yml")
q.each_with_index do |item, i|
  if item["type"] == "mcq"
    raise "q#{i}: answer_index out of range" unless item["answer_index"].between?(0, item["choices"].length - 1)
  elsif item["type"] == "short"
    raise "q#{i}: empty answer_keywords" if item["answer_keywords"].nil? || item["answer_keywords"].empty?
  else
    raise "q#{i}: unknown type #{item["type"]}"
  end
end
puts "schema OK, #{q.length} questions"
'
```
Expected: `schema OK, N questions` with no raised errors.

Run: `bundle exec jekyll build` — expect no errors, and manually confirm (via `bundle exec jekyll serve` and a browser, or by inspecting the built page's embedded `<script type="application/json" id="quiz-data">` block) that the quiz renders with the full question count and grades at least one right/one wrong answer correctly.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/02_cnn_refresher.yml
git commit -m "Add Module 2 quiz data"
```

---

### Task 3: Module 2 coding exercise notebooks

**Files:**
- Create: `notebooks/02-cnn-refresher/exercise_starter.ipynb`
- Create: `notebooks/02-cnn-refresher/exercise_solution.ipynb`

**Interfaces:**
- Follows the exact convention from `notebooks/01-dnn-refresher/` (Colab badge, SMOKE_TEST pattern, starter/solution split with `NotImplementedError` stubs) — read those two files first as your structural template.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 2 Exercise (Solution): Depth, gradients, and residuals on a CIFAR-10 subset", with Colab badge pointing at `exercise_solution.ipynb`, and a link back to the Module 2 page.

Implement, in order:
1. **`Conv2dManual`**: a from-scratch 2D convolution (raw tensor ops — `unfold`/matmul or explicit loops, your choice, favor `unfold`+matmul for reasonable speed) supporting stride and padding, validated against `torch.nn.functional.conv2d` on a random small input (`torch.allclose` within a small tolerance).
2. **Data loading with SMOKE_TEST branch**: when `SMOKE_TEST=1`, use `torch.rand(N, 3, 32, 32)` synthetic tensors with random integer labels (small N, e.g. 256 train / 64 val) instead of downloading CIFAR-10; otherwise use `torchvision.datasets.CIFAR10` (download=True) and subsample to ~10k training images as the spec describes.
3. **`PlainNet(depth)`**: a CNN of conv+BN+ReLU blocks (3x3 convs, no skip connections), parameterized by total depth, supporting depths 8/20/44 (choose a reasonable channel-width scheme, e.g. widen every ~1/3 of the way through like the standard CIFAR ResNet paper's convention, or keep constant width for simplicity — your call, document the choice in a markdown cell).
4. **`ResNet(depth)`**: the same overall depth/channel structure as `PlainNet`, but organized into residual blocks with identity skip connections (`F(x) + x`, with a 1x1 projection shortcut when channel/spatial dims change).
5. **Training loop**: train all 6 models (Plain-8/20/44, ResNet-8/20/44) for a small number of epochs (1 for SMOKE_TEST, ~5-10 for real use), recording per-step or per-epoch training loss AND the gradient norm of the first conv layer's weight (via `.grad.norm()` after `.backward()`, before the optimizer step).
6. **Plotting**: two plots — training loss curves for all 6 models overlaid, and first-layer gradient norm vs. training step for all 6 overlaid (log scale on the gradient-norm y-axis is likely clearer).
7. A closing markdown cell asking the learner to note whether the degradation effect (plain nets getting WORSE as depth increases from 8→44) and the healthy-vs-vanishing gradient contrast actually appear in their run, since with only 1-2 epochs on a subset the effect may be subtle — this is fine, the point is exposure to the phenomenon and the diagnostic technique, not guaranteed dramatic separation.

- [ ] **Step 2: Derive the starter notebook**

Same structure, but stub `Conv2dManual`'s `forward` (or equivalent implementation function), `PlainNet`'s block/forward construction, and `ResNet`'s residual-block/forward construction with `raise NotImplementedError("TODO: ...")` and a one-line hint comment for each, exactly matching the style of `notebooks/01-dnn-refresher/exercise_starter.ipynb`'s stubs. Keep every other cell (data loading, training loop, plotting) identical to the solution so the learner's implementation is exercised by the same harness. Colab badge points at `exercise_starter.ipynb`; header drops "(Solution)".

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m2_solution_out.ipynb notebooks/02-cnn-refresher/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors.

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m2_starter_out.ipynb notebooks/02-cnn-refresher/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError` (correct — proves the stub is on the executed path).

- [ ] **Step 4: Commit**

```bash
git add notebooks/02-cnn-refresher/exercise_starter.ipynb notebooks/02-cnn-refresher/exercise_solution.ipynb
git commit -m "Add Module 2 coding exercise notebooks (starter + solution)"
```

---

### Task 4: Module 3 content pages (index.md + reading.md)

**Files:**
- Create: `modules/03-rnn-lstm/index.md`
- Create: `modules/03-rnn-lstm/reading.md`

**Interfaces:**
- Same pattern as Task 1.

- [ ] **Step 1: Write `modules/03-rnn-lstm/index.md`**

```markdown
---
title: "3. RNNs + LSTMs"
parent: Modules
nav_order: 3
slug: 03-rnn-lstm
has_children: true
---

# Module 3: RNNs + LSTMs

Vanilla RNN, BPTT, vanishing/exploding gradients, LSTM, GRU, seq2seq, Bahdanau/Luong attention.

## Subtopics

- Vanilla RNN recurrence, unrolling, parameter sharing across time
- Backpropagation through time: gradient as a sum over time steps involving repeated Jacobian products
- Truncated BPTT and its bias/tractability tradeoff
- Vanishing/exploding gradients tied to the recurrent weight matrix's spectral norm
- Gradient clipping fixes exploding but not vanishing gradients
- LSTM cell: forget/input/output gates, candidate cell state, additive update as "constant error carousel"
- Why the additive update gives gradient ≈ forget-gate value instead of repeated matrix multiplication
- GRU as a simplified alternative: reset/update gates, merged states
- Bidirectional RNNs and why they can't be used for autoregressive decoding
- Seq2seq: fixed-length context vector, autoregressive decoding, the information-bottleneck problem
- Bahdanau (additive) attention: alignment scores, softmax-weighted context vector
- Luong (multiplicative/dot-product) attention and global vs. local variants
- How RNN-based attention motivates Transformer self-attention (query/key/value framing)

## Reading list

See [Module 3 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"Copy/repeat-delay task: vanilla RNN vs. LSTM, with gradient-flow diagnostics"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/03-rnn-lstm/exercise_starter.ipynb){:target="_blank"}.

Implement a vanilla RNN cell and LSTM cell manually (raw tensor ops, no `nn.RNN`/`nn.LSTM`). Train both on a synthetic copy task across delay lengths; plot accuracy vs. delay (vanilla RNN should collapse past a critical delay; LSTM stays stable) and gradient norm vs. time step at a fixed long delay (exponential decay for vanilla RNN, much flatter for LSTM).

{% include quiz.html slug=page.slug quiz=site.data.quizzes.03_rnn_lstm %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/03-rnn-lstm/reading.md`**

```markdown
---
title: "Reading List"
parent: "3. RNNs + LSTMs"
nav_order: 1
---

# Module 3 Reading List

## Required

- Hochreiter & Schmidhuber (1997). *Long Short-Term Memory.* Neural Computation 9(8). <https://doi.org/10.1162/neco.1997.9.8.1735>
- Cho et al. (2014). *Learning Phrase Representations using RNN Encoder–Decoder (introduces GRU).* <https://arxiv.org/abs/1406.1078>
- Sutskever, Vinyals, Le (2014). *Sequence to Sequence Learning with Neural Networks.* <https://arxiv.org/abs/1409.3215>
- Bahdanau, Cho, Bengio (2014/2015). *Neural Machine Translation by Jointly Learning to Align and Translate.* <https://arxiv.org/abs/1409.0473>
- Luong, Pham, Manning (2015). *Effective Approaches to Attention-based NMT.* <https://arxiv.org/abs/1508.04025>

## Optional / further reading

- Olah (2015). *Understanding LSTM Networks.* <https://colah.github.io/posts/2015-08-Understanding-LSTMs/>
- Alammar (2018). *Visualizing A Neural Machine Translation Model.* <https://jalammar.github.io/visualizing-neural-machine-translation-mechanics-of-seq2seq-models-with-attention/>
- Karpathy (2015). *The Unreasonable Effectiveness of Recurrent Neural Networks.* <http://karpathy.github.io/2015/05/21/rnn-effectiveness/>
- Olah & Carter (2016). *Attention and Augmented Recurrent Neural Networks.* Distill. <https://distill.pub/2016/augmented-rnns/>

## Lecture references

- Stanford CS224n Notes Part V — *Language Models, RNN, GRU and LSTM.* <https://web.stanford.edu/class/cs224n/readings/cs224n-2019-notes05-LM_RNN.pdf>
- Stanford CS224n Notes Part VI — *NMT, Seq2seq and Attention.* <https://web.stanford.edu/class/cs224n/readings/cs224n-2019-notes06-NMT_seq2seq_attention.pdf>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build`
Expected: no errors; `_site/modules/03-rnn-lstm/index.html` and `_site/modules/03-rnn-lstm/reading.html` exist.

- [ ] **Step 4: Commit**

```bash
git add modules/03-rnn-lstm/index.md modules/03-rnn-lstm/reading.md
git commit -m "Add Module 3 (RNNs + LSTMs) content and reading list"
```

---

### Task 5: Module 3 quiz data

**Files:**
- Create: `_data/quizzes/03_rnn_lstm.yml`

**Interfaces:** same pattern as Task 2.

- [ ] **Step 1: Author 8-10 quiz questions**

Cover these topics (from the spec's Module 3 "Quiz topics" list):

1. Identifying the repeated-Jacobian-product term in BPTT that causes vanishing/exploding gradients
2. Why gradient clipping fixes exploding gradients but does nothing for vanishing gradients
3. LSTM gate roles (forget/input/output) — given a scenario, which gate is responsible
4. Why the LSTM's additive cell-state update preserves gradient magnitude over long lags vs. vanilla RNN's multiplicative recurrence
5. GRU vs. LSTM structural differences and when to prefer one
6. Why bidirectional RNNs can't be used for autoregressive/causal generation
7. The fixed-length context vector bottleneck in vanilla seq2seq
8. Bahdanau vs. Luong attention: additive vs. multiplicative scoring
9. Computing attention weights and a context vector from toy query/key vectors or alignment scores (a short numeric/conceptual question)
10. Tracing the line from RNN-based attention to Transformer self-attention — what carries over, what's discarded

Use `_data/quizzes/01_dnn_refresher.yml` as your style reference, same as Task 2.

- [ ] **Step 2: Verify the YAML parses and the page renders**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/03_rnn_lstm.yml').length"`
Expected: a number between 8 and 10.

Run a schema check (every mcq has valid `answer_index`, every short has non-empty `answer_keywords`):
```bash
ruby -ryaml -e '
q = YAML.load_file("_data/quizzes/03_rnn_lstm.yml")
q.each_with_index do |item, i|
  if item["type"] == "mcq"
    raise "q#{i}: answer_index out of range" unless item["answer_index"].between?(0, item["choices"].length - 1)
  elsif item["type"] == "short"
    raise "q#{i}: empty answer_keywords" if item["answer_keywords"].nil? || item["answer_keywords"].empty?
  else
    raise "q#{i}: unknown type #{item["type"]}"
  end
end
puts "schema OK, #{q.length} questions"
'
```
Expected: `schema OK, N questions` with no raised errors.

Run: `bundle exec jekyll build` — expect no errors, and manually confirm (via `bundle exec jekyll serve` and a browser, or by inspecting the built page's embedded `<script type="application/json" id="quiz-data">` block) that the quiz renders with the full question count and grades at least one right/one wrong answer correctly.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/03_rnn_lstm.yml
git commit -m "Add Module 3 quiz data"
```

---

### Task 6: Module 3 coding exercise notebooks

**Files:**
- Create: `notebooks/03-rnn-lstm/exercise_starter.ipynb`
- Create: `notebooks/03-rnn-lstm/exercise_solution.ipynb`

**Interfaces:** follows the same convention as Task 3 / `notebooks/01-dnn-refresher/`.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 3 Exercise (Solution): Copy task — vanilla RNN vs. LSTM, with gradient-flow diagnostics", Colab badge, module link.

Implement, in order:
1. **Synthetic copy-task data generator**: given a delay length `T`, produce sequences where the model must output a short symbol sequence (e.g. 3-5 symbols from a small vocabulary) again after a delay of `T` blank/pad steps — a standard synthetic long-range-dependency benchmark. Support `T` in `{5, 20, 50, 100}` (use small values like `{2, 4}` under `SMOKE_TEST` for speed).
2. **Manual vanilla RNN cell**: raw tensor ops (`h_t = tanh(W_ih @ x_t + W_hh @ h_{t-1} + b)`), looped over time — no `nn.RNN`/`nn.RNNCell`.
3. **Manual LSTM cell**: raw tensor ops implementing the four gates (forget/input/output/candidate) and the additive cell-state update — no `nn.LSTM`/`nn.LSTMCell`.
4. **Training loop**: train both cell types on the copy task across the delay sweep, recording final accuracy per `(model, T)` pair.
5. **Gradient-flow diagnostic**: at a fixed long `T`, use `retain_grad()` on each per-timestep hidden (and, for LSTM, cell) state during a forward pass, backward once, and record `∂L/∂h_t.norm()` for every `t` — plot this against `t` for both model types on the same axes (expect roughly exponential decay for the vanilla RNN, much flatter for LSTM).
6. **Plotting**: accuracy-vs-T for both models (one plot), and the gradient-norm-vs-t diagnostic (a second plot).
7. Closing markdown cell noting the expected qualitative pattern (vanilla RNN collapses past some critical T; LSTM stays stable) and that with the reduced SMOKE_TEST scale the effect may be less pronounced than a full run.

- [ ] **Step 2: Derive the starter notebook**

Stub the RNN cell's forward step and the LSTM cell's forward step (each gate's raw tensor computation) with `raise NotImplementedError("TODO: ...")` and a one-line hint, matching the style of Module 1's starter. Keep the data generator, training loop, gradient-diagnostic, and plotting cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m3_solution_out.ipynb notebooks/03-rnn-lstm/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors.

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m3_starter_out.ipynb notebooks/03-rnn-lstm/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

- [ ] **Step 4: Commit**

```bash
git add notebooks/03-rnn-lstm/exercise_starter.ipynb notebooks/03-rnn-lstm/exercise_solution.ipynb
git commit -m "Add Module 3 coding exercise notebooks (starter + solution)"
```

---

### Task 7: Final verification and deploy

**Files:** none — verification only.

- [ ] **Step 1: Full local build**

Run: `bundle exec jekyll build` — expect no errors.

- [ ] **Step 2: Verify nav reachability for both new modules**

Run: `grep -oE 'href="[^"]*0[23]-[a-z-]+/?"' _site/index.html | sort -u`
Expected: real nav links to both `02-cnn-refresher/` and `03-rnn-lstm/` (this specific check exists because Module 1's nav-invisibility bug was only caught in a very late review — verify it directly here for every new module, every time).

- [ ] **Step 3: Push and confirm CI**

```bash
git push origin master
gh run watch --exit-status
```
Expected: the Pages workflow succeeds. (Note: `_data/**` is in the workflow's trigger paths as of the Plan 1 final-review fixes, so quiz-only commits also deploy correctly.)

- [ ] **Step 4: Verify live**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/02-cnn-refresher/?cb=$(date +%s)"
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/03-rnn-lstm/?cb=$(date +%s)"
```
Expected: both `200`. (Append a cache-busting query param — the live CDN cache can briefly serve a stale copy right after deploy, as observed during Plan 1's final verification.)

Manually confirm (or via grep on the fetched HTML) that each page's quiz section and chat widget section render, matching Module 1's pattern.
