# Modules 11-12 (DETR, CLIP) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Continue Part 3 (Transformers: Vision) — Module 11 (DETR / Object Detection with Transformers) and Module 12 (CLIP / Contrastive Vision-Language Pretraining) — content, reading list, quiz, and coding exercise notebooks, following the pattern established by Modules 1-10.

**Architecture:** Same as the existing site. All shared infrastructure already exists and needs no changes — only content is added. This plan does NOT need any new Python dependency beyond what's already installed: `transformers==4.18.0` supports `DetrForObjectDetection`/`DetrFeatureExtractor` (note: 4.18.0 uses `DetrFeatureExtractor`, NOT `DetrImageProcessor` — that class was added in a later transformers version) and `CLIPModel`/`CLIPProcessor`, confirmed by direct import and checkpoint-load test. `DetrForObjectDetection.from_pretrained` requires the `timm` package (for its CNN backbone) — `timm==0.6.12` is now installed in this environment (confirmed via direct load test of `facebook/detr-resnet-50`, 41.5M params) and must be documented as a new notebook dependency per the existing "Notebook dependencies" convention in `README.md`, which already anticipates `timm` for "later vision modules." `scipy` (for `scipy.optimize.linear_sum_assignment`, the Hungarian algorithm) and `torchmetrics` (for mAP) are also needed for Module 11's exercise — verify both are importable in this environment before Task 3, and add guarded install cells for any that need installing. `openai/clip-vit-base-patch32` was confirmed loadable (151.3M params) with a working `CLIPProcessor`.

**Tech Stack:** Jekyll/Liquid/YAML (content), PyTorch + `torchvision` + `transformers==4.18.0` + `timm` + `scipy` + `torchmetrics` (notebooks), same toolchain as the existing repo.

## Global Constraints

- Every reading-list URL must be copied EXACTLY from `docs/superpowers/specs/2026-07-31-llm-transformers-course-design.md` §4 (Module 11 at line 484, Module 12 at line 520) — no alterations.
- Quiz include syntax must use dot notation (`site.data.quizzes.NN_slug`), never bracket notation.
- New module pages must set `has_children: true`. `reading.md`'s `parent:` must exactly match the module page's `title:`. Module pages nest under `modules/index.md` via `parent: Modules`. `nav_order: 11` and `nav_order: 12` respectively — verify no collision with the existing 1-10 sequence or the (not-yet-created) Module 13.
- Chat widget include added directly in each module's `index.md`.
- Each module's `index.md` should carry a one-line plain-text summary immediately under its H1 heading, in the same comma-separated, period-terminated style established by Modules 1-10 (e.g. Module 9: "Patch embeddings, [CLS] token, DeiT distillation." — this convention was reinforced by Plan 5's final-review fix after Module 10 initially omitted it; do not repeat that omission here).
- Quiz data file naming: hyphenated directory slug, underscored `_data/quizzes/` filename and Liquid key (`11_detr.yml`, `12_clip.yml`).
- Quiz question schema: `{type: mcq, question, choices, answer_index, explanation}` or `{type: short, question, answer_keywords, model_answer, explanation}`.
- **Quiz authoring requirements, automated by `test/quiz-data.test.mjs` (MUST pass — run `node --test test/quiz-data.test.mjs`):**
  - 8-10 questions per file, mcq fraction roughly 70/30 (within the test's tolerance).
  - No bare/loosely-anchored numeric or single-word keywords in `answer_keywords` — route numeric facts to MCQ instead, use multi-word specifically-anchored phrases for any short-answer keyword.
  - `answer_index` values must not all be identical across a file's mcq questions, AND must be reasonably spread across all four positions (0-3) — not just "not all identical." Plan 5's final review flagged `10_swin.yml` for passing the test's minimum bar while still being skewed (index 0 never used, one index used 57% of the time) — treat that as a real quality bar for this plan's two new files, not just the literal test assertion.
  - Correct MCQ choice must not be the longest choice in more than half the file's questions — keep justification prose in `explanation` (shown on both outcomes), keep choice text concise, keep distractors comparably sized.
  - Every short-answer question's `model_answer` must self-grade correct against that question's own `answer_keywords` (also automated by `test/quiz-data.test.mjs`), and must not falsely match a plausible wrong answer — favor long, specifically-anchored phrases (7+ words) over short ones.
- **Notebook dependency convention:** `transformers` is already documented in `README.md`'s "Notebook dependencies" section for modules 05, 08, 09, 10; extend that list to include 11 and 12 in this plan. Follow the identical guarded-install-cell pattern for every new package a notebook needs (`transformers` is already Colab-preinstalled in practice but the guard stays for consistency; `timm`, `scipy`-if-not-preinstalled, and `torchmetrics` each need their own guarded install cell, first code cell after header/badge, e.g. `try:\n    import timm\nexcept ImportError:\n    %pip install -q timm`). `torchvision` needs no guard (Colab always has it). Add all newly-required packages to `README.md`'s "Notebook dependencies" section.
- **Notebook SMOKE_TEST strategy for this plan:** Module 11's Part B (pretrained DETR inference/fine-tuning) and Module 12's both parts (pretrained CLIP zero-shot/retrieval) are fundamentally ABOUT what a real pretrained model does — so `SMOKE_TEST` must use REAL pretrained weights (`DetrForObjectDetection.from_pretrained('facebook/detr-resnet-50')`, `CLIPModel.from_pretrained('openai/clip-vit-base-patch32')`) and only shrink dataset subset size / fine-tuning steps / image-corpus size, never swap in random weights. This matches the established pattern from Modules 8-10, not Module 5's config-only pattern. Module 11 Part A (Hungarian matching from scratch on a synthetic toy scene) has no pretrained-weight dependency at all — it's a small, fully-deterministic synthetic-data exercise, so SMOKE_TEST there just means "the same small synthetic scene runs quickly," which it already does at full scale (3 GT objects, 5 queries is already tiny).
- Notebook convention: paired `exercise_starter.ipynb` / `exercise_solution.ipynb`, nbformat 4 JSON with cell `source` as a **line-list** (not a single string — verify this explicitly after any notebook-editing step; this exact regression has recurred multiple times in this project's history, most recently during Plan 5's final-review fix wave). Colab badge, module link, `SMOKE_TEST` environment variable pattern. Starter must genuinely fail with `NotImplementedError` on the executed path when smoke-tested; solution must smoke-test clean. Keep every non-stubbed cell byte-identical between starter and solution. Notebook `metadata.language_info.version` must be `"3.11"` (matching the established template across all prior modules — do NOT let it default to the authoring machine's actual Python version, which was a Minor finding fixed in Plan 5).
- **Fair-comparison principle (established since Module 2, reinforced by Plan 5's final review):** when comparing pre-/post-fine-tuning DETR (Module 11) or naive/engineered/ensembled CLIP prompts (Module 12), keep confounding factors controlled (same evaluation subset, same budget) so any observed difference is attributable to the thing actually being taught. If a comparison necessarily involves models of different scale or origin, name that difference explicitly in a markdown cell rather than silently omitting it (per Plan 5's fix for Module 9's unacknowledged ~32x parameter gap) — this is not expected to arise in this plan's exercises (both use one pretrained checkpoint each, not a from-scratch-vs-pretrained comparison), but if any such comparison is introduced, it must be disclosed.
- **Correctness-verification discipline (established after Plan 4 caught a real zero-gradient RL bug, and Plan 5 caught a real memory-measurement confound):** for any custom-implemented metric or algorithm, verify its correctness with a concrete check, not just "the code runs." Specifically for this plan:
  - Module 11's from-scratch GIoU implementation must be unit-tested against known cases (identical boxes → IoU=GIoU=1; disjoint boxes far apart → IoU=0 and GIoU approaching -1; partially overlapping boxes with a hand-computed expected value).
  - Module 11's Hungarian-matching permutation-invariance claim must be verified concretely: shuffle the ground-truth object order, re-run matching, and assert the resulting total assigned cost and the underlying box-to-GT pairing (not raw index equality, since indices shift) are identical to the unshuffled run.
  - Module 12's prompt-ensembling implementation must average and RE-NORMALIZE text embeddings across templates (not just average raw logits) before computing cosine similarity — implement it correctly per the CLIP paper's convention and verify with a concrete check (e.g. that ensembled embeddings have unit norm before the similarity computation, and that ensembling with a single template equals the non-ensembled case).

---

## File Structure

```
modules/
  11-detr/{index.md, reading.md}
  12-clip/{index.md, reading.md}
_data/quizzes/
  11_detr.yml
  12_clip.yml
notebooks/
  11-detr/{exercise_starter.ipynb, exercise_solution.ipynb}
  12-clip/{exercise_starter.ipynb, exercise_solution.ipynb}
```

---

### Task 1: Module 11 content pages (index.md + reading.md)

**Files:**
- Create: `modules/11-detr/index.md`
- Create: `modules/11-detr/reading.md`

- [ ] **Step 1: Write `modules/11-detr/index.md`**

```markdown
---
title: "11. DETR (Object Detection with Transformers)"
parent: Modules
nav_order: 11
slug: 11-detr
has_children: true
---

# Module 11: DETR — Object Detection with Transformers

Set prediction, object queries, bipartite matching, and the end of NMS.

## Subtopics

- Object detection reframed as direct set prediction, vs. anchor-based (Faster R-CNN) and NMS-dependent (YOLO) pipelines
- What anchors/NMS/proposals solve in classical detectors
- DETR architecture: CNN backbone → flattened feature map → transformer encoder → transformer decoder → parallel FFN heads
- Spatial positional encodings for 2D CNN feature maps
- Object queries: fixed learned decoder-input slots, each specializing via cross-attention
- Decoder self-attention among queries enabling implicit de-duplication (replacing NMS)
- Bipartite matching via the Hungarian algorithm between predictions and padded ground truth
- Matching cost: classification cost + box L1 + generalized IoU (GIoU); why plain IoU is insufficient (zero gradient when non-overlapping)
- Hungarian/set-prediction loss: matched-pair classification NLL + box L1/GIoU, summed over the optimal assignment
- No-object (∅) class and down-weighting its loss for class imbalance
- NMS/anchor elimination as a consequence of one-to-one matching (permutation invariance)
- Known limitations (slow convergence, weak small-object detection, quadratic attention over large feature maps) → Deformable DETR

## Reading list

See [Module 11 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"Hungarian matching from scratch + applied pretrained DETR"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/11-detr/exercise_starter.ipynb){:target="_blank"}.

Part A: build a synthetic toy scene (3 ground-truth objects, 5 predicted queries), implement GIoU from scratch and unit-test it, build the matching cost matrix, solve with `scipy.optimize.linear_sum_assignment`, compute the full Hungarian/set-prediction loss, and verify permutation invariance under GT reordering. Part B: load `facebook/detr-resnet-50`, run inference, visualize predicted boxes and object-query cross-attention maps, and fine-tune on a small detection subset.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.11_detr %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/11-detr/reading.md`**

```markdown
---
title: "Reading List"
parent: "11. DETR (Object Detection with Transformers)"
nav_order: 1
---

# Module 11 Reading List

## Required

- Carion, Massa, Synnaeve, Usunier, Kirillov, Zagoruyko (2020). *End-to-End Object Detection with Transformers.* <https://arxiv.org/abs/2005.12872>
- Zhu, Su, Lu, Li, Wang, Dai (2021, ICLR Oral). *Deformable DETR.* <https://arxiv.org/abs/2010.04159>
- Meta AI Blog. *End-to-end object detection with Transformers.* <https://ai.meta.com/blog/end-to-end-object-detection-with-transformers/>
- Model card: `facebook/detr-resnet-50`. <https://huggingface.co/facebook/detr-resnet-50>

## Optional / further reading

- DigitalOcean. *Introduction to DETR — Part 2: The Hungarian Algorithm.* <https://www.digitalocean.com/community/tutorials/introduction-detr-hungarian-algorithm-2>
- HF Paper Page for DETR. <https://huggingface.co/papers/2005.12872>
- Model card: `facebook/detr-resnet-50-panoptic`. <https://huggingface.co/facebook/detr-resnet-50-panoptic>

## Lecture references

- Stanford CS231n Lecture 9, *Object Detection, Image Segmentation...* (lists DETR among suggested readings). <https://cs231n.stanford.edu/schedule.html>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; `_site/modules/11-detr/index.html` and `reading.html` exist.

- [ ] **Step 4: Commit**

```bash
git add modules/11-detr/index.md modules/11-detr/reading.md
git commit -m "Add Module 11 (DETR) content and reading list"
```

---

### Task 2: Module 11 quiz data

**Files:**
- Create: `_data/quizzes/11_detr.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/08_efficient_transformers.yml`, `09_vit.yml`, and `10_swin.yml` as your style/quality/fairness reference — these (and all quiz files) are currently balanced per `test/quiz-data.test.mjs`'s invariants. Pay particular attention to `answer_index` spread across all 4 positions, per this plan's Global Constraints (Plan 5's final review flagged `10_swin.yml` for a skewed-but-passing distribution — do better here).

Cover these topics (from the spec's Module 11 "Quiz topics"):
1. Why set prediction needs an assignment step
2. What NMS solves and why DETR doesn't need it
3. Anchor boxes' purpose vs. DETR's lack of them
4. Object-query behavior (learned, fixed decoder-input slots specializing via cross-attention)
5. Why Hungarian (optimal) matching over greedy heuristics
6. The three matching-cost components and why L1 alone is insufficient
7. Why GIoU over plain IoU (zero-gradient problem for non-overlapping boxes)
8. No-object class purpose and down-weighting
9. Spatial positional encodings for CNN feature maps
10. DETR's known weaknesses and Deformable DETR's fix

- [ ] **Step 2: Verify**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/11_detr.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file's schema, keyword-safety, answer-index-variety, question-count, mcq-ratio, answer-length-balance, and model_answer-self-grading checks.

Run `bundle exec jekyll build` — expect no errors; spot-check the quiz renders with the right question count.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/11_detr.yml
git commit -m "Add Module 11 quiz data"
```

---

### Task 3: Module 11 coding exercise notebooks

**Files:**
- Create: `notebooks/11-detr/exercise_starter.ipynb`
- Create: `notebooks/11-detr/exercise_solution.ipynb`

**Interfaces:**
- Uses `transformers==4.18.0` (`DetrForObjectDetection`, `DetrFeatureExtractor` — NOT `DetrImageProcessor`, which doesn't exist in this version), `timm` (confirmed installed, `0.6.12`, required by `DetrForObjectDetection.from_pretrained` for its CNN backbone), `scipy.optimize.linear_sum_assignment` (verify importable — it's part of `scipy`, check whether a guarded install cell is needed or if it's already available), and `torchmetrics` (for mAP — verify importable, add a guarded install cell if not already present in this environment). Guarded install cells for any of `timm`/`scipy`/`torchmetrics` that need installing, as the first code cells (matching the `transformers` guard pattern from `notebooks/05-llm-lineage/` etc.). Per Global Constraints, Part B's SMOKE_TEST uses real pretrained DETR weights, shrinking only the fine-tuning subset/steps.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 11 Exercise (Solution): Hungarian matching from scratch + applied pretrained DETR", Colab badge, module link.

Implement, in order:
1. **Part A setup**: construct a synthetic toy scene — 3 ground-truth boxes (with class labels) and 5 predicted queries (with class logits and box predictions), in normalized `(cx, cy, w, h)` format, with a KNOWN correct optimal assignment (i.e. construct predictions so it's clear by design which 3 of the 5 queries should match which GT box, and which 2 should match "no object").
2. **GIoU from scratch**: implement `box_iou(boxes1, boxes2)` and `generalized_box_iou(boxes1, boxes2)` (convert cx/cy/w/h → x1/y1/x2/y2 first), matching the standard GIoU formula (IoU minus the normalized area of the smallest enclosing box not covered by either box). **Unit-test it** against 3 known cases: (a) identical boxes → IoU=GIoU=1.0 (assert `torch.allclose`); (b) two boxes far apart with no shared enclosing-box slack → IoU=0 and GIoU strictly negative (assert both); (c) a partially-overlapping pair where you hand-compute the expected IoU and GIoU values from the box coordinates and assert `torch.allclose` against your hand-computed numbers (show the hand computation in a markdown cell or code comment).
3. **Matching cost matrix**: build the 5×3 cost matrix combining classification cost (negative predicted probability of the GT class) + box L1 cost + GIoU cost (with documented relative weights, e.g. matching the DETR paper's λ_cls=1, λ_L1=5, λ_giou=2).
4. **Solve with `scipy.optimize.linear_sum_assignment`**: get the optimal assignment, print which queries matched which GT boxes (or no-object), and confirm it matches the scene's by-construction known-correct assignment.
5. **Full Hungarian/set-prediction loss**: compute the matched-pair classification NLL + box L1/GIoU loss summed over the optimal assignment, plus the down-weighted no-object loss for unmatched queries.
6. **Permutation invariance verification (concrete, not just asserted)**: shuffle the GT box order, re-run cost-matrix construction + matching, and assert that (a) the total assigned cost is numerically identical (`torch.allclose` or `==` within float tolerance) to the unshuffled run, and (b) the actual box-to-GT semantic pairing (not raw indices, which will differ after shuffling — compare by box coordinates/class identity) is unchanged.
7. **Part B — pretrained DETR inference**: load `facebook/detr-resnet-50` via `DetrForObjectDetection.from_pretrained(...)` and `DetrFeatureExtractor.from_pretrained(...)`, run inference on 2-3 real images (bundle a couple of small sample images or use a couple of CIFAR-10/COCO-sample images resized appropriately — document the image source), visualize predicted boxes with class labels and confidence scores overlaid.
8. **Object-query cross-attention visualization**: extract and visualize the decoder cross-attention map for a few object queries that matched real objects (transformers 4.18.0's `DetrForObjectDetection` supports `output_attentions=True` — verify this works and extract the relevant attention tensor).
9. **Fine-tuning arm**: fine-tune the loaded DETR on a small custom/synthetic or COCO-subset detection set for a few steps (SMOKE_TEST: minimal steps/tiny subset; full: a slightly larger subset), and compare pre-/post-fine-tuning predictions qualitatively (visualized boxes) and via `torchmetrics`'s mAP metric on a held-out mini eval set.
10. Closing markdown cell summarizing what set prediction + Hungarian matching buys over anchor/NMS pipelines.

- [ ] **Step 2: Derive the starter notebook**

Stub `generalized_box_iou`, the matching-cost-matrix construction, and the full Hungarian/set-prediction loss computation, each with `raise NotImplementedError("TODO: ...")` and a one-line hint. Keep the synthetic scene setup, unit tests, permutation-invariance check, and Part B inference/fine-tuning/comparison cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m11_solution_out.ipynb notebooks/11-detr/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors; the GIoU unit tests, matching-assignment check, and permutation-invariance check all genuinely assert success (not silently pass on a trivial case); the pretrained-DETR arm genuinely downloads/uses real pretrained weights (confirm via a printed checkpoint name or realistic download evidence, not a random-init fallback).

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m11_starter_out.ipynb notebooks/11-detr/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

Verify cell `source` fields are line-lists (not single strings) in both files after saving. Verify `metadata.language_info.version` is `"3.11"` in both.

- [ ] **Step 4: Commit**

```bash
git add notebooks/11-detr/exercise_starter.ipynb notebooks/11-detr/exercise_solution.ipynb
git commit -m "Add Module 11 coding exercise notebooks (starter + solution)"
```

---

### Task 4: Module 12 content pages (index.md + reading.md)

**Files:**
- Create: `modules/12-clip/index.md`
- Create: `modules/12-clip/reading.md`

- [ ] **Step 1: Write `modules/12-clip/index.md`**

```markdown
---
title: "12. CLIP (Contrastive Vision-Language Pretraining)"
parent: Modules
nav_order: 12
slug: 12-clip
has_children: true
---

# Module 12: CLIP — Contrastive Vision-Language Pretraining

Joint image-text embeddings, symmetric contrastive loss, and zero-shot classification via prompts.

## Subtopics

- Joint image-text embedding space via projection into a shared multimodal vector space
- Dual-encoder architecture: independent image encoder (ResNet/ViT) + text encoder (Transformer over BPE), each with a linear projection into the shared dim
- Symmetric contrastive (InfoNCE-style) loss over an N×N batch similarity matrix, averaged image→text and text→image
- Cosine similarity between L2-normalized embeddings; the learned temperature/logit-scale parameter
- Large-scale, noisy, web-collected image-text pairs (~400M) substituting for costly curated labels
- Zero-shot classification via prompt engineering ("a photo of a {class}") and scoring against candidate class text embeddings
- Prompt ensembling (averaging ~80 templates) for robustness
- Open-vocabulary recognition with zero task-specific fine-tuning
- Zero-shot vs. linear-probe evaluation on frozen CLIP features
- Downstream uses: DALL-E 2/Stable Diffusion conditioning, retrieval, general-purpose visual features
- Known limitations: typographic attacks, inherited web-data bias, weak fine-grained/counting/spatial reasoning
- Why large batch sizes matter for contrastive pretraining (in-batch negatives)

## Reading list

See [Module 12 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"Zero-shot classification and retrieval with pretrained CLIP"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/12-clip/exercise_starter.ipynb){:target="_blank"}.

Part A: load `openai/clip-vit-base-patch32`, build several prompt templates per class over a small labeled set (5-6 CIFAR-10 classes), classify via cosine similarity, implement and compare naive vs. engineered vs. ensembled prompt accuracy. Part B: assemble a small unlabeled image corpus, precompute CLIP image embeddings, implement text-to-image and image-to-image retrieval, and try free-text queries outside the original label set to show open-vocabulary retrieval.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.12_clip %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/12-clip/reading.md`**

```markdown
---
title: "Reading List"
parent: "12. CLIP (Contrastive Vision-Language Pretraining)"
nav_order: 1
---

# Module 12 Reading List

## Required

- Radford, Kim, Hallacy et al. (2021). *Learning Transferable Visual Models From Natural Language Supervision (CLIP).* <https://arxiv.org/abs/2103.00020>
- Jia, Yang, Xia et al. (2021). *ALIGN.* <https://arxiv.org/abs/2102.05918>
- Cherti, Beaumont, Wightman et al. (2022). *Reproducible Scaling Laws for Contrastive Language-Image Learning (OpenCLIP).* <https://arxiv.org/abs/2212.07143>

## Optional / further reading

- Ramesh, Dhariwal, Nichol, Chu, Chen (2022). *DALL-E 2 (Hierarchical Text-Conditional Image Generation with CLIP Latents).* <https://arxiv.org/abs/2204.06125>
- Roboflow. *What is CLIP?* <https://blog.roboflow.com/openai-clip/>
- Wikipedia. *Contrastive Language-Image Pre-training.* <https://en.wikipedia.org/wiki/Contrastive_Language-Image_Pre-training>

## Lecture references

- Stanford CS231n — Lecture 12 *Self-Supervised Learning*, Lecture 16 *Vision and Language*. <https://cs231n.stanford.edu/schedule.html>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; both pages exist in `_site/`.

- [ ] **Step 4: Commit**

```bash
git add modules/12-clip/index.md modules/12-clip/reading.md
git commit -m "Add Module 12 (CLIP) content and reading list"
```

---

### Task 5: Module 12 quiz data

**Files:**
- Create: `_data/quizzes/12_clip.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/09_vit.yml` through `11_detr.yml` as style/quality references. Per this plan's Global Constraints, ensure `answer_index` spread is genuinely balanced across all 4 positions, not just passing the test's minimum bar.

Cover these topics (from the spec's Module 12 "Quiz topics"):
1. Why CLIP's contrastive loss is symmetric over both batch directions
2. Role of the learned temperature (logit-scale) parameter
3. Zero-shot classification via natural-language prompts
4. Why prompt engineering/ensembling changes accuracy with frozen weights
5. Zero-shot vs. linear-probe evaluation
6. Why large batch sizes matter for contrastive pretraining (in-batch negatives)
7. Noisy web-scale vs. curated data tradeoffs
8. Open-vocabulary recognition vs. fixed-head classifiers
9. Downstream uses of CLIP embeddings
10. Known CLIP failure modes (typographic attacks, bias, weak fine-grained/counting/spatial reasoning)

- [ ] **Step 2: Verify**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/12_clip.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file.

Run `bundle exec jekyll build` — expect no errors; spot-check rendering.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/12_clip.yml
git commit -m "Add Module 12 quiz data"
```

---

### Task 6: Module 12 coding exercise notebooks

**Files:**
- Create: `notebooks/12-clip/exercise_starter.ipynb`
- Create: `notebooks/12-clip/exercise_solution.ipynb`

**Interfaces:**
- Uses `transformers==4.18.0` (`CLIPModel`, `CLIPProcessor`, both confirmed installed and loadable via `openai/clip-vit-base-patch32`, 151.3M params) and `torchvision` (CIFAR-10, for the labeled classification subset). No new install guard needed beyond `transformers`'s existing guard pattern (CLIP doesn't need `timm`). SMOKE_TEST uses real pretrained weights, shrinking only subset/corpus sizes.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 12 Exercise (Solution): Zero-shot classification and retrieval with pretrained CLIP", Colab badge, module link.

Implement, in order:
1. **Setup**: load `CLIPModel.from_pretrained('openai/clip-vit-base-patch32')` and `CLIPProcessor.from_pretrained(...)`, load a small CIFAR-10 subset covering 5-6 classes (SMOKE_TEST: small; full: larger, still modest since this is inference-only, no training).
2. **Naive prompts**: classify using just the bare class name as the "prompt" (e.g. `"cat"`), compute image-text cosine similarity scaled by the model's `logit_scale`, classify via argmax, record accuracy.
3. **Engineered prompt**: classify using a single engineered template (e.g. `"a photo of a {class}"`), record accuracy, compare to naive.
4. **Prompt ensembling — implement correctly, not just averaged logits**: build ~5-10 diverse templates (a reasonable subset representative of CLIP's published ~80-template ensemble, e.g. `"a photo of a {class}"`, `"a blurry photo of a {class}"`, `"a photo of a small {class}"`, etc.), compute each template's L2-normalized text embedding per class, AVERAGE the embeddings across templates and RE-NORMALIZE the averaged embedding to unit norm (this is the correct CLIP-paper convention — averaging then renormalizing, not averaging raw similarity scores/logits), then classify via cosine similarity against the ensembled embeddings. **Concretely verify correctness**: (a) assert the ensembled per-class embeddings have unit L2 norm (within float tolerance) after renormalization; (b) assert that ensembling with a single-template list degenerates exactly to the non-ensembled single-template case (`torch.allclose`).
5. **Compare naive vs. engineered vs. ensembled accuracy** in a table/bar chart; discuss why accuracy differs despite frozen weights (the model's zero-shot classification is entirely a function of how well the prompt text embedding matches the true class's semantic direction in the joint space).
6. **Part B — retrieval**: assemble a small unlabeled image corpus (e.g. a broader CIFAR-10 sample spanning multiple classes, or another small public image set already available in this environment — document the source), precompute CLIP image embeddings for the whole corpus, implement `retrieve(query_text, k)` (embed the query text, cosine-similarity rank against all precomputed image embeddings, return top-k) and `retrieve_similar_images(query_image, k)` (same but with an image query).
7. **Open-vocabulary demonstration**: try 2-3 free-text queries describing concepts NOT in CIFAR-10's original 10 labels (e.g. compositional descriptions like "a red vehicle" or "an animal with fur") and show the retrieved results qualitatively, to demonstrate CLIP's open-vocabulary behavior beyond any fixed label set.
8. Closing markdown cell summarizing what zero-shot + open-vocabulary buys over a fixed-head classifier, and where CLIP's known limitations would show up (fine-grained/counting/spatial reasoning, typographic attacks).

- [ ] **Step 2: Derive the starter notebook**

Stub the prompt-ensembling embedding-averaging-and-renormalization logic and the `retrieve`/`retrieve_similar_images` functions, each with `raise NotImplementedError("TODO: ...")` and a one-line hint. Keep setup, naive/engineered prompt cells, the ensembling correctness checks (once un-stubbed they'd need the implementation — so these checks live in cells that depend on the stubbed function and will naturally fail via the propagated `NotImplementedError`, which is fine and expected for the starter), and the open-vocabulary demonstration cells identical to the solution except for the required stubs.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m12_solution_out.ipynb notebooks/12-clip/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors; the ensembling unit-norm and single-template-degeneracy checks both genuinely assert success; retrieval returns sensible (or at least well-formed) top-k results; CLIP genuinely uses real pretrained weights.

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m12_starter_out.ipynb notebooks/12-clip/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

Verify cell `source` fields are line-lists in both files after saving. Verify `metadata.language_info.version` is `"3.11"` in both.

- [ ] **Step 4: Commit**

```bash
git add notebooks/12-clip/exercise_starter.ipynb notebooks/12-clip/exercise_solution.ipynb
git commit -m "Add Module 12 coding exercise notebooks (starter + solution)"
```

---

### Task 7: Final verification and deploy

**Files:** none — verification only.

- [ ] **Step 1: Full local build**

Run: `bundle exec jekyll build` — expect no errors.

- [ ] **Step 2: Verify nav reachability for both new modules**

```bash
grep -o 'href="[^"]*11-detr/*"' _site/index.html | sort -u
grep -o 'href="[^"]*12-clip/*"' _site/index.html | sort -u
```
Expected: both produce at least one real nav link.

- [ ] **Step 3: Update README.md's Notebook dependencies section**

Add `notebooks/11-detr/` (needs `transformers`, `timm`, possibly `scipy`/`torchmetrics` if they needed guards) and `notebooks/12-clip/` (needs `transformers`) to the list alongside the existing 05/08/09/10 entries.

- [ ] **Step 4: Run the full automated test suite**

Run: `node --test test/*.test.mjs` — expect all pass, including `quiz-data.test.mjs` covering both new quiz files.

Run: `cd worker && npm test` — expect all pass (unaffected by this plan, confirm no regression).

- [ ] **Step 5: Verify each new module's quiz and chat widget render**

For each of the two new modules, confirm (via the same JSON-extraction approach used in Plan 5's Task 7, not a naive HTML grep — the site is minified single-line HTML) the embedded quiz JSON parses to 8-10 questions and `chat-widget-root` appears on the page.

- [ ] **Step 6: Commit the README update**

```bash
git add README.md
git commit -m "Document Module 11-12 notebook dependencies in README"
```

- [ ] **Step 7: Push and confirm CI**

```bash
git push origin master
```
Then poll `gh run list --limit 3` for the newly-triggered "Build and deploy Pages" run and watch it: `gh run watch <run-id> --exit-status`.
Expected: the Pages workflow succeeds. (The Worker deploy workflow will also trigger since it touches `modules/**`; its deploy step is gated to skip, not fail, when `CLOUDFLARE_API_TOKEN` isn't configured — this has consistently shown as a `failure`-labeled-but-0-second gated skip in this repo's run history since Plan 2, not a real failure — expected, not a blocker.)

- [ ] **Step 8: Verify live**

Run, with cache-busting query params:
```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/11-detr/?cb=$(date +%s)"
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/12-clip/?cb=$(date +%s)"
```
Expected: both `200`.
