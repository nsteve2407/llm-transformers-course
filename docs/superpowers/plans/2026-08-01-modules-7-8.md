# Modules 7-8 (RL for LLMs, Efficient Transformers) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete Part 2 (Transformers: Text) — Module 7 (RL for LLMs: RLHF/PPO/DPO/GRPO/RLVR) and Module 8 (Efficient Transformers: FlashAttention/KV-caching/quantization) — content, reading list, quiz, and coding exercise notebooks, following the pattern established by Modules 1-6.

**Architecture:** Same as the existing site. All shared infrastructure already exists and needs no changes — only content is added.

**Tech Stack:** Jekyll/Liquid/YAML (content), PyTorch/Jupyter (Module 7's from-scratch exercise), PyTorch + `transformers==4.18.0` (Module 8's KV-cache benchmark), same toolchain as the existing repo.

## Global Constraints

- Every reading-list URL must be copied EXACTLY from `docs/superpowers/specs/2026-07-31-llm-transformers-course-design.md` §4 (Module 7 at line 327, Module 8 at line 367) — no alterations.
- Quiz include syntax must use dot notation (`site.data.quizzes.NN_slug`), never bracket notation.
- New module pages must set `has_children: true`. `reading.md`'s `parent:` must exactly match the module page's `title:`. Module pages nest under `modules/index.md` via `parent: Modules`.
- Chat widget include added directly in each module's `index.md`.
- Quiz data file naming: hyphenated directory slug, underscored `_data/quizzes/` filename and Liquid key.
- Quiz question schema: `{type: mcq, question, choices, answer_index, explanation}` or `{type: short, question, answer_keywords, model_answer, explanation}`.
- **Quiz authoring lessons from Modules 1-6 (all now enforced by `test/quiz-data.test.mjs`, which MUST pass — run `node --test test/quiz-data.test.mjs` as part of every quiz task's verification):**
  - Avoid bare/loosely-anchored numeric keywords in `answer_keywords` — route numeric facts to MCQ format instead (this project's established, successful pattern since Module 4).
  - `answer_index` values across a file's mcq questions must not all be identical (varied position, no positional "always pick option N" tell).
  - The correct MCQ choice should not be the longest choice in more than roughly half the file's questions — keep justification/reasoning prose in the `explanation` field (which the quiz UI displays on both correct and incorrect outcomes), not padded into the correct choice text itself. Keep distractors reasonably close in length to the correct choice.
  - Every short-answer question's `model_answer` should itself grade correct against that question's own `answer_keywords` (not yet automated in `quiz-data.test.mjs` — verify manually per question with a quick Node script, same as prior modules' quiz tasks).
- **Notebook dependency convention (established after Module 5, documented in `README.md`'s "Notebook dependencies" section):** any notebook needing a package beyond what Colab preinstalls by default must add a guarded install cell as its first code cell (right after the header/badge markdown cell): `try:\n    import <package>\nexcept ImportError:\n    %pip install -q <package>`. `transformers` is preinstalled on Colab and already used this way in `notebooks/05-llm-lineage/`; Module 8's notebook should follow the identical pattern.
- Notebook convention: paired `exercise_starter.ipynb` / `exercise_solution.ipynb`, nbformat 4 JSON with cell `source` as a **line-list** (not a single string — Module 6 had to be fixed for this after the fact; write it correctly the first time), Colab badge, module link, `SMOKE_TEST` environment variable pattern. Starter must genuinely fail with `NotImplementedError` on the executed path when smoke-tested; solution must smoke-test clean. Keep every non-stubbed cell byte-identical between starter and solution.
- Every module's `_data/quizzes/<slug>.yml` should have 8-10 questions, mixing `mcq` and `short` (roughly 70/30), covering the module's full "Quiz topics" list from the spec.

---

## File Structure

```
modules/
  07-rl-for-llms/{index.md, reading.md}
  08-efficient-transformers/{index.md, reading.md}
_data/quizzes/
  07_rl_for_llms.yml
  08_efficient_transformers.yml
notebooks/
  07-rl-for-llms/{exercise_starter.ipynb, exercise_solution.ipynb}
  08-efficient-transformers/{exercise_starter.ipynb, exercise_solution.ipynb}
```

---

### Task 1: Module 7 content pages (index.md + reading.md)

**Files:**
- Create: `modules/07-rl-for-llms/index.md`
- Create: `modules/07-rl-for-llms/reading.md`

- [ ] **Step 1: Write `modules/07-rl-for-llms/index.md`**

```markdown
---
title: "7. RL for LLMs"
parent: Modules
nav_order: 7
slug: 07-rl-for-llms
has_children: true
---

# Module 7: RL for LLMs

RLHF (reward model + PPO), DPO, GRPO, RLVR/reasoning-model training.

## Subtopics

- The three-stage RLHF pipeline (SFT → reward model → RL fine-tuning) and why it's staged rather than end-to-end
- Bradley-Terry preference model and the reward model's pairwise logistic loss
- PPO's clipped surrogate objective as a trust-region approximation without second-order optimization
- The KL penalty against a frozen reference policy (reward = RM score − β·KL) and why unconstrained RL against a learned RM degenerates
- Value/critic network and GAE in PPO; the need for a token-level value head for LLM PPO
- InstructGPT's concrete pipeline (demonstration data → comparison data → PPO) at production scale
- DPO's reparameterization: closed-form optimal policy collapses RLHF into a single classification-style loss, no explicit RM or rollouts
- DPO (offline, static preference data) vs. PPO-RLHF (online, on-policy sampling) tradeoffs
- GRPO: removing the critic by sampling a group of completions per prompt and using group-relative advantages
- RLVR: rule-based/verifiable rewards (math correctness, unit tests, format checks) replacing a learned RM, sidestepping reward hacking
- DeepSeek-R1 recipe: R1-Zero (pure RL, no SFT) vs. R1 (cold-start SFT → large-scale RL → rejection sampling → final RL), emergent long CoT
- Reward hacking / over-optimization as a general failure mode, and how KL penalties / RM ensembling / verifiable rewards each mitigate it

## Reading list

See [Module 7 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"DPO from scratch on a toy verifiable-reward task"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/07-rl-for-llms/exercise_starter.ipynb){:target="_blank"}.

A from-scratch tiny character-level transformer over a digit vocabulary, with a rule-based verifiable reward (count of adjacent ascending digits). Generate synthetic preference pairs ranked by the reward; pretrain and freeze a reference policy; run a minimal PPO loop (clipped surrogate + KL penalty vs. reference); separately implement the DPO loss directly on (prompt, chosen, rejected) triplets; evaluate and compare, then ablate DPO's β and PPO's KL coefficient toward 0 to observe reward hacking/mode collapse.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.07_rl_for_llms %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/07-rl-for-llms/reading.md`**

```markdown
---
title: "Reading List"
parent: "7. RL for LLMs"
nav_order: 1
---

# Module 7 Reading List

## Required

- Ouyang et al. (2022). *Training language models to follow instructions with human feedback (InstructGPT).* <https://arxiv.org/abs/2203.02155>
- Schulman, Wolski, Dhariwal, Radford, Klimov (2017). *Proximal Policy Optimization Algorithms.* <https://arxiv.org/abs/1707.06347>
- Rafailov, Sharma, Mitchell, Ermon, Manning, Finn (2023). *Direct Preference Optimization.* <https://arxiv.org/abs/2305.18290>
- Shao et al. (2024). *DeepSeekMath (introduces GRPO).* <https://arxiv.org/abs/2402.03300>
- DeepSeek-AI (2025). *DeepSeek-R1.* <https://arxiv.org/abs/2501.12948>

## Optional / further reading

- Weng. *Policy Gradient Algorithms.* <https://lilianweng.github.io/posts/2018-04-08-policy-gradient/>
- Hugging Face. *Illustrating RLHF.* <https://huggingface.co/blog/rlhf>
- Bai et al. (2022, Anthropic). *Training a Helpful and Harmless Assistant with RLHF.* <https://arxiv.org/abs/2204.05862>
- Hugging Face TRL docs, *GRPO Trainer.* <https://huggingface.co/docs/trl/grpo_trainer>
- Lambert (Interconnects). *DeepSeek R1's recipe to replicate o1.* <https://www.interconnects.ai/p/deepseek-r1-recipe-for-o1>

## Lecture references

- Stanford CS224N Sp2024 Lecture 10, "Post-training (RLHF, SFT, DPO)". <https://web.stanford.edu/class/archive/cs/cs224n/cs224n.1246/>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; `_site/modules/07-rl-for-llms/index.html` and `reading.html` exist.

- [ ] **Step 4: Commit**

```bash
git add modules/07-rl-for-llms/index.md modules/07-rl-for-llms/reading.md
git commit -m "Add Module 7 (RL for LLMs) content and reading list"
```

---

### Task 2: Module 7 quiz data

**Files:**
- Create: `_data/quizzes/07_rl_for_llms.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/04_attention.yml`, `05_llm_lineage.yml`, and `06_scaling_modern_llms.yml` as your style/quality references — these were re-balanced for answer-position and answer-length fairness after a final review caught issues in the earlier Module 4-6 batch; match that improved standard directly (don't reproduce the original mistake of always-longest-correct-choice or constant-answer-position).

Cover these topics (from the spec's Module 7 "Quiz topics"):
1. Why PPO uses a KL penalty against a reference policy in RLHF
2. Bradley-Terry model assumptions underlying reward-model training
3. DPO's implicit reward vs. an explicit, separately-trained reward model
4. Why GRPO drops the critic/value network, and how group-relative advantage is computed
5. What makes a reward "verifiable" in RLVR, and where it breaks down (open-ended tasks)
6. How PPO's clipped surrogate prevents destructively large policy updates
7. On-policy sampling (PPO) vs. static offline preference data (DPO)
8. Reward hacking / reward over-optimization against a learned reward model
9. The SFT stage's dual role as initialization and reference policy
10. DeepSeek-R1-Zero (pure RL) vs. DeepSeek-R1's multi-stage pipeline

Route numeric facts (if any arise, e.g. specific hyperparameter values) to MCQ format per the Global Constraints.

- [ ] **Step 2: Verify the YAML parses, passes the automated invariant test, and the page renders**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/07_rl_for_llms.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file.

Write and run a Node script testing `gradeShortAnswer` (from `assets/js/quiz-logic.mjs`) against adversarial answers for any keyword you're unsure about, and confirm every short-answer question's `model_answer` self-grades correct against that question's own `answer_keywords`.

Run `bundle exec jekyll build` — expect no errors; spot-check the quiz renders with the right question count.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/07_rl_for_llms.yml
git commit -m "Add Module 7 quiz data"
```

---

### Task 3: Module 7 coding exercise notebooks

**Files:**
- Create: `notebooks/07-rl-for-llms/exercise_starter.ipynb`
- Create: `notebooks/07-rl-for-llms/exercise_solution.ipynb`

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 7 Exercise (Solution): DPO from scratch on a toy verifiable-reward task", Colab badge, module link. Write cell `source` fields as line-lists (not single strings) from the start.

Implement, in order:
1. **Toy setup**: a small vocabulary of digits (0-9) plus a few control tokens (BOS/EOS/PAD), a tiny 2-layer, 2-head causal character-level Transformer (reuse the causal self-attention pattern from `notebooks/04-attention/` or `notebooks/06-scaling-modern-llms/` — small enough to train in minutes on CPU or a free T4).
2. **Verifiable reward function**: given a generated digit sequence, compute a rule-based score (count of adjacent ascending digit pairs, e.g. "1357" scores 3, "9513" scores 0) — implement this as a pure function with a few hand-checked test cases in a cell (e.g. `assert reward_fn([1,3,5,7]) == 3`).
3. **Reference policy pretraining**: pretrain the tiny transformer via next-token cross-entropy on a mix of random and partially-sorted digit sequences (standing in for an SFT stage), then freeze a copy as `pi_ref`.
4. **Synthetic preference pair generation**: sample multiple completions per prompt from the reference policy, score each with the reward function, and construct (prompt, chosen, rejected) triplets by picking a higher-reward and lower-reward completion per prompt.
5. **(Optional but implement it) Bradley-Terry reward model**: a small scalar-head model trained with the pairwise logistic loss `-log sigmoid(r(chosen) - r(rejected))` on the preference pairs — OR skip the learned reward model and use the rule-based reward function directly for the PPO loop (document whichever choice you make and why; using the rule-based reward directly is simpler and still demonstrates RLVR-style training, which is explicitly in this module's scope).
6. **Minimal PPO loop**: sample completions from the current policy, compute per-token advantages (a simple baseline is fine — e.g. reward minus a running mean, or a lightweight value head, your choice, document it), apply the clipped surrogate objective, add a KL penalty term against `pi_ref` (estimated via log-prob ratio), and step the optimizer. Track reward-per-step across training.
7. **DPO loss**: implement `-log sigmoid(beta * ((logpi(chosen) - logpi_ref(chosen)) - (logpi(rejected) - logpi_ref(rejected))))` directly on the (prompt, chosen, rejected) triplets from the SAME frozen `pi_ref` checkpoint used in step 6, training a separate copy of the policy.
8. **Evaluation and comparison**: evaluate `pi_ref`, the PPO-tuned policy, and the DPO-tuned policy on held-out prompts, plot reward distributions for each.
9. **Ablation**: sweep DPO's `beta` toward a low value and PPO's KL coefficient toward 0, and observe/plot the resulting reward-hacking or mode-collapse behavior (e.g. reward climbing while output diversity/quality collapses — you can operationalize "quality collapse" simply, e.g. by checking if outputs degenerate to a single repeated digit).
10. Closing markdown cell summarizing what was demonstrated (RLHF-style PPO pipeline, DPO's simpler single-loss alternative, and empirical reward hacking when the KL/beta regularization is weakened).

Use a `SMOKE_TEST` branch (small model, few training steps, small dataset) for fast verification; full runs (SMOKE_TEST unset) use a larger training budget.

- [ ] **Step 2: Derive the starter notebook**

Stub the reward function, the PPO clipped-surrogate-plus-KL loss computation, and the DPO loss computation, each with `raise NotImplementedError("TODO: ...")` and a one-line hint. Keep model definition, preference-pair generation, training loops' surrounding structure, evaluation, and plotting cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m7_solution_out.ipynb notebooks/07-rl-for-llms/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors, and the reward-function unit-test assertions actually pass (not silently skipped).

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m7_starter_out.ipynb notebooks/07-rl-for-llms/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

- [ ] **Step 4: Commit**

```bash
git add notebooks/07-rl-for-llms/exercise_starter.ipynb notebooks/07-rl-for-llms/exercise_solution.ipynb
git commit -m "Add Module 7 coding exercise notebooks (starter + solution)"
```

---

### Task 4: Module 8 content pages (index.md + reading.md)

**Files:**
- Create: `modules/08-efficient-transformers/index.md`
- Create: `modules/08-efficient-transformers/reading.md`

- [ ] **Step 1: Write `modules/08-efficient-transformers/index.md`**

```markdown
---
title: "8. Efficient Transformers"
parent: Modules
nav_order: 8
slug: 08-efficient-transformers
has_children: true
---

# Module 8: Efficient Transformers

FlashAttention, KV caching, quantization, RoPE-extension for long context.

## Subtopics

- FlashAttention's tiling: splitting Q/K/V into SRAM-sized blocks to avoid materializing the full N×N attention matrix in HBM
- IO-awareness: attention is memory-bandwidth-bound, not FLOP-bound, on GPUs
- Online softmax: numerically stable incremental softmax via running max/sum rescaling
- Recomputation-based backward pass instead of storing the O(N²) matrix
- FlashAttention-2's refinements: reduced non-matmul FLOPs, better GPU work partitioning
- KV caching: turning each decode step from O(seq²) into O(seq) incremental work; prefill vs. decode asymmetry
- KV cache memory formula: 2 × batch × seq_len × layers × heads × head_dim × bytes
- MQA and GQA as KV-cache-size reduction techniques
- GPTQ's layer-wise post-training quantization minimizing per-layer reconstruction error via approximate Hessian info
- bitsandbytes' LLM.int8(): outlier-feature-aware mixed int8/fp16 matmul
- 4-bit NF4 quantization (QLoRA): quantile-based dtype optimal for normally-distributed weights, double quantization
- RoPE's poor extrapolation beyond trained context length, and YaRN's NTK-aware interpolation fix

## Reading list

See [Module 8 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"KV-cache benchmark for autoregressive generation"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/08-efficient-transformers/exercise_starter.ipynb){:target="_blank"}.

Load a small causal LM (GPT-2 small). Implement generation without KV caching (full forward pass every step) and with KV caching (incremental). Benchmark both across generation lengths, plot time and peak memory vs. sequence length, and verify the KV-cache memory formula against measured numbers.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.08_efficient_transformers %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/08-efficient-transformers/reading.md`**

```markdown
---
title: "Reading List"
parent: "8. Efficient Transformers"
nav_order: 1
---

# Module 8 Reading List

## Required

- Dao, Fu, Ermon, Rudra, Ré (2022). *FlashAttention.* <https://arxiv.org/abs/2205.14135>
- Ainslie et al. (2023). *GQA.* <https://arxiv.org/abs/2305.13245>
- Frantar, Ashkboos, Hoefler, Alistarh (2022). *GPTQ.* <https://arxiv.org/abs/2210.17323>
- Dettmers, Lewis, Belkada, Zettlemoyer (2022). *LLM.int8().* <https://arxiv.org/abs/2208.07339>
- Su, Lu, Pan, Murtadha, Wen, Liu (2021). *RoFormer.* <https://arxiv.org/abs/2104.09864>
- Peng, Quesnelle, Fan, Shippole (2023). *YaRN.* <https://arxiv.org/abs/2309.00071>

## Optional / further reading

- Shazeer (2019). *Fast Transformer Decoding: One Write-Head is All You Need (MQA).* <https://arxiv.org/abs/1911.02150>
- Press, Smith, Lewis (2021). *ALiBi.* <https://arxiv.org/abs/2108.12409>
- Hugging Face blog, *4-bit quantization and QLoRA.* <https://huggingface.co/blog/4bit-transformers-bitsandbytes>
- PyTorch blog, *Out of the box acceleration... with PyTorch 2.0.* <https://pytorch.org/blog/out-of-the-box-acceleration/>
- Hugging Face docs, *KV cache strategies.* <https://huggingface.co/docs/transformers/en/kv_cache>

## Lecture references

- Stanford CS336, *Language Modeling from Scratch* (Inference lecture + FlashAttention2 Triton assignment). <https://cs336.stanford.edu/>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; both pages exist in `_site/`.

- [ ] **Step 4: Commit**

```bash
git add modules/08-efficient-transformers/index.md modules/08-efficient-transformers/reading.md
git commit -m "Add Module 8 (Efficient Transformers) content and reading list"
```

---

### Task 5: Module 8 quiz data

**Files:**
- Create: `_data/quizzes/08_efficient_transformers.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/04_attention.yml` through `07_rl_for_llms.yml` as style/quality references. This module (KV cache memory formula, quantization bit-widths, GQA group ratios) has heavy numeric content — route numeric facts to MCQ format per the Global Constraints, following the successful pattern from Modules 4-7.

Cover these topics (from the spec's Module 8 "Quiz topics"):
1. Why FlashAttention is IO-aware not FLOP-reducing
2. How tiling + online softmax avoid materializing the full N×N attention matrix
3. Why FlashAttention's backward pass recomputes rather than stores attention blocks
4. How KV cache memory scales with batch size, seq length, layers, heads, head dim
5. How GQA interpolates between MHA and MQA, and the resulting tradeoff
6. GPTQ's use of approximate Hessian information to minimize quantization error
7. What NF4 quantization normalizes for, and why it suits normally-distributed weights
8. Weight-only vs. weight+activation quantization, and why activation outliers complicate LLM.int8()
9. Why RoPE-based models struggle to extrapolate past trained context length
10. How YaRN treats low- vs. high-frequency RoPE dimensions differently vs. naive linear interpolation

- [ ] **Step 2: Verify the YAML parses, passes the automated invariant test, and the page renders**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/08_efficient_transformers.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file.

Write and run a Node script testing `gradeShortAnswer` against adversarial answers for any keyword you're unsure about, and confirm every short-answer question's `model_answer` self-grades correct.

Run `bundle exec jekyll build` — expect no errors; spot-check rendering.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/08_efficient_transformers.yml
git commit -m "Add Module 8 quiz data"
```

---

### Task 6: Module 8 coding exercise notebooks

**Files:**
- Create: `notebooks/08-efficient-transformers/exercise_starter.ipynb`
- Create: `notebooks/08-efficient-transformers/exercise_solution.ipynb`

**Interfaces:**
- Uses `transformers==4.18.0` (already installed). Follow the guarded-install-cell convention from the Global Constraints (first code cell, matching `notebooks/05-llm-lineage/`'s pattern). Unlike Module 5, this exercise's whole point is measuring REAL inference speed/memory characteristics, so — unlike Module 5's SMOKE_TEST-gated config-only loading — this notebook should default to loading real pretrained GPT-2 weights even under `SMOKE_TEST`, since GPT-2 small (~124M params, ~500MB) is a manageable download and the exercise has no meaningful "random weights" analogue (timing/memory benchmarks need a real model doing real work, similar to how Module 12's future CLIP exercise will also need real weights). Use `SMOKE_TEST` instead to reduce the generation-length sweep (fewer/shorter lengths) and batch size, not to swap in a fake model.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 8 Exercise (Solution): KV-cache benchmark for autoregressive generation", Colab badge, module link. Write cell `source` fields as line-lists from the start. Guarded install cell for `transformers` as the first code cell.

Implement, in order:
1. **Load GPT-2 small** (`GPT2LMHeadModel.from_pretrained("gpt2")`, fp16 if CUDA is available else fp32, `.eval()`) and its tokenizer.
2. **Generation without KV caching**: implement a generation loop that, at each step, re-runs the full forward pass over the entire sequence-so-far (`use_cache=False`), appending the newly sampled/argmax token each time.
3. **Generation with KV caching**: implement a generation loop using `past_key_values` (`use_cache=True`), passing only the newest token at each step after the initial prefill.
4. **Benchmark**: for a sweep of generation lengths (`SMOKE_TEST`: e.g. `[8, 16, 32]`; full: `[32, 64, 128, 256, 512]`), time both approaches (use `torch.cuda.Event` timing if on GPU, else `time.perf_counter`) and record `torch.cuda.max_memory_allocated()` (reset via `torch.cuda.reset_peak_memory_stats()` before each run) if CUDA is available, else note that peak-memory measurement is CUDA-only and skip it gracefully on CPU.
5. **Plotting**: time-vs-generation-length for both approaches overlaid (no-cache should show faster-than-linear growth, cached should show closer-to-linear), and peak-memory-vs-length if CUDA was available.
6. **Verify the KV-cache memory formula**: compute the theoretical KV-cache size in bytes using the formula `2 * batch * seq_len * num_layers * num_heads * head_dim * bytes_per_element` with GPT-2's actual config values (`model.config.n_layer`, `n_head`, `n_embd` to derive `head_dim = n_embd / n_head`), and compare against the measured memory delta attributable to the cache (this comparison is necessarily approximate since measured memory includes other overhead — document that the theoretical number should be a reasonable fraction of the measured peak, not an exact match, and explain why).
7. **Extension**: recompute the theoretical KV-cache size under hypothetical MQA (1 KV head) and GQA (e.g. 4 groups) configurations instead of GPT-2's full MHA, and print the resulting size reduction, connecting back to the GQA paper's motivation.
8. Closing markdown cell summarizing the quadratic-vs-linear generation-time contrast and the KV-cache memory tradeoff.

- [ ] **Step 2: Derive the starter notebook**

Stub the no-cache generation loop's per-step forward-pass logic and the with-cache generation loop's `past_key_values` handling, each with `raise NotImplementedError("TODO: ...")` and a hint. Keep model/tokenizer loading, benchmarking harness, plotting, and the memory-formula verification cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m8_solution_out.ipynb notebooks/08-efficient-transformers/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors. Note this WILL download real GPT-2 weights (~500MB) — confirm this is acceptable/expected (unlike Module 5's exercise, this one's SMOKE_TEST only reduces sweep size, not model fidelity, per the Interfaces note above).

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m8_starter_out.ipynb notebooks/08-efficient-transformers/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

- [ ] **Step 4: Commit**

```bash
git add notebooks/08-efficient-transformers/exercise_starter.ipynb notebooks/08-efficient-transformers/exercise_solution.ipynb
git commit -m "Add Module 8 coding exercise notebooks (starter + solution)"
```

---

### Task 7: Final verification and deploy

**Files:** none — verification only.

- [ ] **Step 1: Full local build**

Run: `bundle exec jekyll build` — expect no errors.

- [ ] **Step 2: Verify nav reachability for both new modules**

Run: `grep -oE 'href="[^"]*0[78]-[a-z-]+/?"' _site/index.html | sort -u`
Expected: real nav links to `07-rl-for-llms/` and `08-efficient-transformers/`.

- [ ] **Step 3: Run the full automated test suite**

Run: `node --test test/*.test.mjs` — expect all pass, including `quiz-data.test.mjs` covering both new quiz files.

Run: `cd worker && npm test` — expect all pass (unaffected by this plan, but confirm no regression).

- [ ] **Step 4: Verify each new module's quiz and chat widget render**

For each of the two new modules, confirm (via grep on the built HTML) the embedded quiz JSON parses to 8-10 questions and `chat-widget-root` appears on the page.

- [ ] **Step 5: Push and confirm CI**

```bash
git push origin master
gh run watch --exit-status
```
Expected: the Pages workflow succeeds. (The Worker deploy workflow will also trigger on this push since it touches `modules/**`; per the existing skip-not-fail gating, its deploy step will be skipped, not failed, if `CLOUDFLARE_API_TOKEN` isn't configured yet — that's expected, don't treat it as a blocker.)

- [ ] **Step 6: Verify live**

Run, with cache-busting query params:
```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/07-rl-for-llms/?cb=$(date +%s)"
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/08-efficient-transformers/?cb=$(date +%s)"
```
Expected: both `200`.
