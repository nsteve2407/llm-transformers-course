# Modules 9-10 (Vision Transformer, Swin) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Begin Part 3 (Transformers: Vision) — Module 9 (Vision Transformer / ViT) and Module 10 (Hierarchical ViTs / Swin) — content, reading list, quiz, and coding exercise notebooks, following the pattern established by Modules 1-8.

**Architecture:** Same as the existing site. All shared infrastructure already exists and needs no changes — only content is added. This plan does NOT need any new Python dependency: `transformers==4.18.0` (already installed and documented) supports both `ViTForImageClassification`/`ViTModel` and `SwinForImageClassification`/`SwinModel`, confirmed by direct import test. `torchvision` (already installed) provides CIFAR-10 loading.

**Tech Stack:** Jekyll/Liquid/YAML (content), PyTorch + `torchvision` + `transformers==4.18.0` (notebooks), same toolchain as the existing repo.

## Global Constraints

- Every reading-list URL must be copied EXACTLY from `docs/superpowers/specs/2026-07-31-llm-transformers-course-design.md` §4 (Module 9 at line 410, Module 10 at line 448) — no alterations. Module 10 explicitly has NO lecture references in the spec ("none found specifically dedicated to Swin — flagged rather than guessed") — do not invent one; omit the "Lecture references" section entirely from Module 10's reading.md, or state it explicitly says none were found (match the spec's own framing).
- Quiz include syntax must use dot notation (`site.data.quizzes.NN_slug`), never bracket notation.
- New module pages must set `has_children: true`. `reading.md`'s `parent:` must exactly match the module page's `title:`. Module pages nest under `modules/index.md` via `parent: Modules`.
- Chat widget include added directly in each module's `index.md`.
- Quiz data file naming: hyphenated directory slug, underscored `_data/quizzes/` filename and Liquid key.
- Quiz question schema: `{type: mcq, question, choices, answer_index, explanation}` or `{type: short, question, answer_keywords, model_answer, explanation}`.
- **Quiz authoring requirements, now automated by `test/quiz-data.test.mjs` (MUST pass — run `node --test test/quiz-data.test.mjs`):**
  - 8-10 questions per file, mcq fraction roughly 70/30 (within the test's tolerance).
  - No bare/loosely-anchored numeric or single-word keywords in `answer_keywords` — route numeric facts to MCQ instead, use multi-word specifically-anchored phrases for any short-answer keyword.
  - `answer_index` values must not all be identical across a file's mcq questions.
  - Correct MCQ choice must not be the longest choice in more than half the file's questions — keep justification prose in `explanation` (shown on both outcomes), keep choice text concise, keep distractors comparably sized.
  - Every short-answer question's `model_answer` must self-grade correct against that question's own `answer_keywords` (also now automated by `test/quiz-data.test.mjs` — verify by running the test, no separate manual script needed for this specific check, though you should still adversarially test any keyword you're genuinely unsure about).
- **Notebook dependency convention:** `transformers` is already documented in `README.md`'s "Notebook dependencies" section and used with a guarded install cell in `notebooks/05-llm-lineage/` and `notebooks/08-efficient-transformers/`. Follow the identical guarded-install-cell pattern in both new notebooks (first code cell, after header/badge): `try:\n    import transformers\nexcept ImportError:\n    %pip install -q transformers`. `torchvision` is a PyTorch-ecosystem package Colab always has — no guard needed for it, consistent with prior modules' CIFAR-10 notebooks (02).
- **Notebook SMOKE_TEST strategy for this plan:** unlike Module 5 (random-weight config-only loading) and matching Module 8's approach (real weights, smaller workload), Modules 9-10's exercises are fundamentally ABOUT what pretrained weights buy you (ViT's data-hunger, Swin's efficiency) — so `SMOKE_TEST` here should use REAL pretrained weights (`ViTForImageClassification.from_pretrained(...)`, `SwinForImageClassification.from_pretrained(...)`) and only shrink the CIFAR-10 subset size / epoch count / fine-tuning steps, never swap in random weights. This mirrors Module 8's established, successful pattern — follow it, not Module 5's.
- Notebook convention: paired `exercise_starter.ipynb` / `exercise_solution.ipynb`, nbformat 4 JSON with cell `source` as a **line-list** (not a single string — verify this explicitly after any notebook-editing step, since this exact regression has recurred twice in this project's history, in Module 6 and again during a Plan 4 quiz-length-balancing pass that incidentally touched notebook cells). Colab badge, module link, `SMOKE_TEST` environment variable pattern. Starter must genuinely fail with `NotImplementedError` on the executed path when smoke-tested; solution must smoke-test clean. Keep every non-stubbed cell byte-identical between starter and solution.
- **Fair-comparison principle (established since Module 2):** when comparing from-scratch ViT vs. CNN baseline vs. fine-tuned pretrained ViT (Module 9), or fine-tuned Swin vs. fine-tuned ViT (Module 10), keep confounding factors controlled (comparable parameter counts where the comparison calls for it, same dataset subset, same training budget) so any observed difference is attributable to the thing actually being taught, not an unrelated architecture/setup difference.
- **Gradient/loss correctness discipline (added after Plan 4's final review caught a real zero-gradient bug in Module 7):** for any exercise involving a custom loss or training signal, don't just verify the code "runs" — verify the training signal actually has the properties claimed. For Module 9/10's exercises this is lower-risk (standard cross-entropy classification, no novel loss), but the shifted-window attention masking in Module 10 has a real correctness bar: verify the mask genuinely prevents attention leakage between non-adjacent cyclically-shifted regions with a concrete numerical check, not just "the code compiles."

---

## File Structure

```
modules/
  09-vit/{index.md, reading.md}
  10-swin/{index.md, reading.md}
_data/quizzes/
  09_vit.yml
  10_swin.yml
notebooks/
  09-vit/{exercise_starter.ipynb, exercise_solution.ipynb}
  10-swin/{exercise_starter.ipynb, exercise_solution.ipynb}
```

---

### Task 1: Module 9 content pages (index.md + reading.md)

**Files:**
- Create: `modules/09-vit/index.md`
- Create: `modules/09-vit/reading.md`

- [ ] **Step 1: Write `modules/09-vit/index.md`**

```markdown
---
title: "9. Vision Transformer (ViT)"
parent: Modules
nav_order: 9
slug: 09-vit
has_children: true
---

# Module 9: Vision Transformer (ViT)

Patch embeddings, [CLS] token, DeiT distillation.

## Subtopics

- Image-to-patch tokenization: splitting H×W×C into N=HW/P² patches, flattening
- Linear projection of patches into embedding dim (equivalent to Conv2d(kernel=stride=P))
- Learnable [CLS] token (borrowed from BERT) as pooled classification representation
- 1D learnable position embeddings vs. 2D/sinusoidal alternatives — ablations found little benefit from 2D-aware schemes
- Standard pre-norm transformer encoder block reused unchanged from NLP transformers
- Inductive-bias comparison with CNNs: loss of locality/translation-equivariance, substituted by large-scale pretraining
- Data-efficiency gap: ViT underperforms CNNs from scratch on small/mid datasets
- Attention map visualization and attention rollout
- Hybrid ViT: CNN (ResNet) stem producing feature-map "patches" instead of raw pixels
- Fine-tuning at higher resolution + 2D interpolation of position embeddings
- DeiT's distillation token: second learnable token trained via a teacher's hard-label predictions
- DeiT's data-efficiency recipe: RandAugment, Mixup, CutMix, stochastic depth

## Reading list

See [Module 9 Reading List](reading.html) for required and optional papers, plus lecture references.

## Coding exercise

**"ViT from scratch vs. CNN baseline vs. fine-tuned pretrained ViT, on CIFAR-10"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/09-vit/exercise_starter.ipynb){:target="_blank"}.

Build patch embedding, [CLS] token + learned position embeddings, and pre-norm encoder blocks for a small "ViT-Tiny". Train from scratch on CIFAR-10 (no pretraining), compare against a similarly-sized CNN baseline and a fine-tuned pretrained ViT — illustrating ViT's data hunger and how pretraining buys back the missing inductive bias.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.09_vit %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/09-vit/reading.md`**

```markdown
---
title: "Reading List"
parent: "9. Vision Transformer (ViT)"
nav_order: 1
---

# Module 9 Reading List

## Required

- Dosovitskiy et al. (2020/2021). *An Image is Worth 16x16 Words (ViT).* <https://arxiv.org/abs/2010.11929>
- Touvron, Cord, Douze, Massa, Sablayrolles, Jégou (2020/2021). *DeiT.* <https://arxiv.org/abs/2012.12877>
- Abnar & Zuidema (2020). *Quantifying Attention Flow in Transformers (attention rollout).* <https://arxiv.org/abs/2005.00928>
- Steiner, Kolesnikov, Zhai, Wightman, Uszkoreit, Beyer (2021). *How to train your ViT?* <https://arxiv.org/abs/2106.10270>

## Optional / further reading

- Google Research Blog. *Transformers for Image Recognition at Scale.* <https://research.google/blog/transformers-for-image-recognition-at-scale/>
- *Dive into Deep Learning*, §11.8, *Transformers for Vision.* <https://d2l.ai/chapter_attention-mechanisms-and-transformers/vision-transformer.html>

## Lecture references

- Stanford CS25 V1, *Transformers in Vision* (Lucas Beyer). <https://web.stanford.edu/class/cs25/past/cs25-v1/> · video: <https://www.youtube.com/watch?v=BP5CM0YxbP8>
- Stanford CS231n Lecture 8, *Self-Attention and Transformers.* <https://cs231n.stanford.edu/schedule.html>
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; `_site/modules/09-vit/index.html` and `reading.html` exist.

- [ ] **Step 4: Commit**

```bash
git add modules/09-vit/index.md modules/09-vit/reading.md
git commit -m "Add Module 9 (Vision Transformer) content and reading list"
```

---

### Task 2: Module 9 quiz data

**Files:**
- Create: `_data/quizzes/09_vit.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/06_scaling_modern_llms.yml`, `07_rl_for_llms.yml`, and `08_efficient_transformers.yml` as your style/quality/fairness reference — these (and all quiz files) are currently balanced per `test/quiz-data.test.mjs`'s invariants.

Cover these topics (from the spec's Module 9 "Quiz topics"):
1. Why ViT needs more data/pretraining than CNNs to match accuracy
2. Role and mechanics of the [CLS] token
3. ViT position-embedding design choices (1D learned vs. 2D/sinusoidal alternatives)
4. Patch-size tradeoffs (sequence length vs. per-patch information)
5. Which inductive biases CNNs have that ViT lacks (locality, translation equivariance, weight sharing)
6. Attention rollout vs. raw single-layer attention maps
7. DeiT's distillation token vs. simple logit averaging
8. Position-embedding interpolation at higher fine-tuning resolution
9. Hybrid ViT (CNN stem + transformer) vs. pure-patch ViT
10. DeiT's specific augmentation/regularization recipe (RandAugment, Mixup, CutMix, stochastic depth)

- [ ] **Step 2: Verify**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/09_vit.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file's schema, keyword-safety, answer-index-variety, question-count, mcq-ratio, answer-length-balance, and model_answer-self-grading checks.

Run `bundle exec jekyll build` — expect no errors; spot-check the quiz renders with the right question count.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/09_vit.yml
git commit -m "Add Module 9 quiz data"
```

---

### Task 3: Module 9 coding exercise notebooks

**Files:**
- Create: `notebooks/09-vit/exercise_starter.ipynb`
- Create: `notebooks/09-vit/exercise_solution.ipynb`

**Interfaces:**
- Uses `transformers==4.18.0` (`ViTForImageClassification`, confirmed already installed and importable) and `torchvision` (CIFAR-10). Guarded install cell for `transformers` as the first code cell (matching `notebooks/05-llm-lineage/`/`notebooks/08-efficient-transformers/`). Per the Global Constraints, `SMOKE_TEST` shrinks the CIFAR-10 subset/epoch count but ALWAYS uses real pretrained weights for the "fine-tuned ViT" comparison arm.

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 9 Exercise (Solution): ViT from scratch vs. CNN baseline vs. fine-tuned pretrained ViT, on CIFAR-10", Colab badge, module link.

Implement, in order:
1. **Data loading with SMOKE_TEST branch**: CIFAR-10 via `torchvision.datasets.CIFAR10`, subsampled (`SMOKE_TEST`: e.g. 512 train / 128 val images; full: e.g. 10k/2k), resized to 32×32 (native) for the from-scratch models and to 224×224 (via `torchvision.transforms.Resize`) for the pretrained-ViT fine-tuning arm (pretrained ViT checkpoints expect 224×224 inputs — document this resizing requirement explicitly).
2. **Patch embedding**: implement BOTH the `unfold`+`Linear` form and the equivalent `Conv2d(kernel=stride=P)` form for patch size P=4 on 32×32 images (N=64 patches), and verify numerically that both produce the same output given matched weights (`torch.allclose`) — this mirrors Module 2's `Conv2dManual`-vs-`F.conv2d` validation pattern.
3. **ViT-Tiny from scratch**: [CLS] token prepended, learned 1D position embeddings, 6 pre-norm encoder blocks (4 heads, head dim 48, so `d_model=192`), MLP head off the final [CLS] token. Target ~2-3M params (verify actual param count in a cell, don't just assert the target).
4. **CNN baseline**: a comparably-sized small CNN (reuse ideas from Module 2's `ConvNet`/`ResNet` skeleton if helpful) — document the parameter-count comparison explicitly (doesn't need to be exact, but should be in the same order of magnitude, documented as a judgment call).
5. **Train both from scratch** on the CIFAR-10 subset for a small number of epochs, plot loss/accuracy curves for both.
6. **Fine-tune a pretrained ViT**: load `google/vit-base-patch16-224` (or a similarly available small ViT checkpoint — verify it's actually downloadable/loadable in this environment before committing to it) via `ViTForImageClassification.from_pretrained(...)`, replace/adapt the classification head for CIFAR-10's 10 classes, fine-tune for a few epochs on the same (resized) subset, record accuracy.
7. **Compare all three**: a table or bar chart of final accuracy (from-scratch ViT vs. CNN baseline vs. fine-tuned pretrained ViT) — expected pattern (more visible at full scale than smoke scale): from-scratch ViT lags the CNN baseline; fine-tuned pretrained ViT wins clearly.
8. **Attention rollout (implement this, not just optional per the spec's "optional" framing — it's a good concrete deliverable)**: implement attention rollout (recursively multiplying averaged attention matrices across layers, accounting for the residual connection per Abnar & Zuidema) for the from-scratch ViT, and visualize the [CLS] token's rolled-out attention over the input patches for a couple of correctly- and incorrectly-classified examples.
9. Closing markdown cell summarizing the data-efficiency-gap finding and what it demonstrates about inductive bias vs. scale.

- [ ] **Step 2: Derive the starter notebook**

Stub the patch embedding functions (both forms), the ViT-Tiny encoder block's forward pass, and the attention rollout computation, each with `raise NotImplementedError("TODO: ...")` and a one-line hint. Keep data loading, CNN baseline, training loops, the pretrained fine-tuning cell, comparison, and plotting cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m9_solution_out.ipynb notebooks/09-vit/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors, the patch-embedding `torch.allclose` validation actually asserts success, and the fine-tuned-ViT arm genuinely downloads/uses real pretrained weights (verify this concretely, e.g. by confirming a realistic download/load time or a printed confirmation of the checkpoint name, not a random-init fallback).

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m9_starter_out.ipynb notebooks/09-vit/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

Verify cell `source` fields are line-lists (not single strings) in both files after saving.

- [ ] **Step 4: Commit**

```bash
git add notebooks/09-vit/exercise_starter.ipynb notebooks/09-vit/exercise_solution.ipynb
git commit -m "Add Module 9 coding exercise notebooks (starter + solution)"
```

---

### Task 4: Module 10 content pages (index.md + reading.md)

**Files:**
- Create: `modules/10-swin/index.md`
- Create: `modules/10-swin/reading.md`

- [ ] **Step 1: Write `modules/10-swin/index.md`**

```markdown
---
title: "10. Hierarchical ViTs (Swin Transformer)"
parent: Modules
nav_order: 10
slug: 10-swin
has_children: true
---

# Module 10: Hierarchical ViTs (Swin Transformer)

## Subtopics

- Why global MSA is O((HW)²) in patch count and prohibitive for dense high-res tasks
- Windowed MSA (W-MSA): non-overlapping M×M windows, within-window attention only
- Complexity: O((HW)²) global vs. O(HW·M²) linear windowed — deriving the FLOPs
- Shifted-window MSA (SW-MSA): alternating regular/displaced window layouts for cross-window connections at linear cost
- The cyclic-shift trick (`torch.roll`) for efficient batched SW-MSA vs. padding
- Masking to prevent attention leakage between non-adjacent regions cyclically shifted into the same window
- Relative position bias vs. absolute embeddings; Swin V2's continuous position bias
- Patch merging: 2×2 neighbor concat + linear projection, halving resolution/doubling channels per stage
- 4-stage hierarchical architecture mirroring CNN feature pyramids
- Swin block structure: LN→W-MSA/SW-MSA→residual→LN→MLP→residual, always in W/SW pairs
- Swin as a drop-in backbone for dense prediction (Mask R-CNN, UperNet)
- Empirical comparison vs. plain ViT/DeiT and ResNets on ImageNet-1K/COCO/ADE20K

## Reading list

See [Module 10 Reading List](reading.html) for required and optional papers. No lecture reference specifically dedicated to Swin was found during course design — see the reading list for details.

## Coding exercise

**"Implement shifted-window attention from scratch + fine-tune Swin vs. ViT"** — [Open the starter notebook in Colab](https://colab.research.google.com/github/nsteve2407/llm-transformers-course/blob/master/notebooks/10-swin/exercise_starter.ipynb){:target="_blank"}.

Implement `window_partition`/`window_reverse`, windowed attention with a relative position bias table, and shifted-window attention (roll + region-id mask + reverse roll), unit-tested against a naive brute-force reference. Then fine-tune pretrained Swin-Tiny and a comparably-sized ViT on a CIFAR-10 subset and compare accuracy, params, memory, and throughput — including at higher resolution, to expose Swin's linear vs. ViT's quadratic scaling.

{% include quiz.html slug=page.slug quiz=site.data.quizzes.10_swin %}

{% include chat-widget.html slug=page.slug title=page.title %}
```

- [ ] **Step 2: Write `modules/10-swin/reading.md`**

```markdown
---
title: "Reading List"
parent: "10. Hierarchical ViTs (Swin Transformer)"
nav_order: 1
---

# Module 10 Reading List

## Required

- Liu et al. (2021, ICCV Best Paper). *Swin Transformer.* <https://arxiv.org/abs/2103.14030>
- Liu et al. *Swin Transformer V2.* <https://arxiv.org/abs/2111.09883>
- Hugging Face docs, *Swin Transformer.* <https://huggingface.co/docs/transformers/model_doc/swin>
- Model card: `microsoft/swin-tiny-patch4-window7-224`. <https://huggingface.co/microsoft/swin-tiny-patch4-window7-224>

## Optional / further reading

- Arora. *Swin Transformer Explained.* <https://amaarora.github.io/posts/2022-07-04-swintransformerv1.html>
- Lightly.ai. *Swin Explained: An Overview.* <https://www.lightly.ai/blog/swin-transformer>
- Official repo. <https://github.com/microsoft/Swin-Transformer>

No lecture reference specifically dedicated to Swin/hierarchical ViTs was found and verified during course design — this was flagged rather than guessed; rely on the readings above.
```

- [ ] **Step 3: Verify locally**

Run: `bundle exec jekyll build` — expect no errors; both pages exist in `_site/`.

- [ ] **Step 4: Commit**

```bash
git add modules/10-swin/index.md modules/10-swin/reading.md
git commit -m "Add Module 10 (Swin Transformer) content and reading list"
```

---

### Task 5: Module 10 quiz data

**Files:**
- Create: `_data/quizzes/10_swin.yml`

- [ ] **Step 1: Author 8-10 questions**

Read `_data/quizzes/07_rl_for_llms.yml` through `09_vit.yml` as style/quality references.

Cover these topics (from the spec's Module 10 "Quiz topics"):
1. Why global MSA scales quadratically and its dense-prediction cost
2. W-MSA vs. SW-MSA and why alternating is necessary
3. Purpose of the cyclic shift and why naive re-windowing after shifting creates irregular windows
4. How the attention mask prevents non-adjacent regions from attending after the shift
5. Why relative position bias is used per-window instead of absolute embeddings
6. How patch merging builds a hierarchical/pyramidal representation
7. Why Swin's complexity is linear vs. ViT's quadratic (FLOPs derivation)
8. Internal structure of a Swin block and its parallel to standard transformer blocks
9. Why Swin suits detection/segmentation backbones better than plain ViT
10. Accuracy/compute/inductive-bias tradeoffs among Swin, ViT, and ResNets

- [ ] **Step 2: Verify**

Run: `ruby -ryaml -e "puts YAML.load_file('_data/quizzes/10_swin.yml').length"` — expect 8-10.

Run: `node --test test/quiz-data.test.mjs` — expect all tests pass, including the new file.

Run `bundle exec jekyll build` — expect no errors; spot-check rendering.

- [ ] **Step 3: Commit**

```bash
git add _data/quizzes/10_swin.yml
git commit -m "Add Module 10 quiz data"
```

---

### Task 6: Module 10 coding exercise notebooks

**Files:**
- Create: `notebooks/10-swin/exercise_starter.ipynb`
- Create: `notebooks/10-swin/exercise_solution.ipynb`

**Interfaces:**
- Uses `transformers==4.18.0` (`SwinForImageClassification`, `ViTForImageClassification`, both confirmed already installed and importable) and `torchvision`. Guarded install cell for `transformers`. `SMOKE_TEST` shrinks dataset/epoch scale but always uses real pretrained weights (same policy as Module 9).

- [ ] **Step 1: Implement the solution notebook**

Title: "Module 10 Exercise (Solution): Shifted-window attention from scratch + fine-tuned Swin vs. ViT", Colab badge, module link.

Implement, in order:
1. **Part A — from-scratch shifted-window attention**:
   - `window_partition(x, window_size)` / `window_reverse(windows, window_size, H, W)` for a feature map of shape `(B, H, W, C)`.
   - `WindowAttention`: QKV projection, scaled dot-product attention within each window, multi-head split, a relative position bias table (learned, indexed by relative patch offsets within a window) added to the attention scores before softmax.
   - Shifted-window mechanics: `torch.roll` by `(-window_size//2, -window_size//2)`, an attention mask (built from a region-id map so that attention is only allowed within originally-adjacent regions post-shift, applied as an additive -inf mask before softmax), then reverse-roll after attention.
   - **Unit test against a naive brute-force reference**: implement a slow, explicit-Python-loop-over-windows reference (no batching tricks) with IDENTICAL weights, and assert `torch.allclose` agreement for both W-MSA and SW-MSA across at least 3 different `(H, W, window_size)` combinations, INCLUDING at least one non-divisible edge case (H or W not evenly divisible by window_size, requiring padding).
   - **Concrete masking correctness check** (per this plan's Global Constraints on verifying training-signal/masking correctness, not just "it runs"): after computing SW-MSA's attention weights, verify numerically that patches from non-adjacent (pre-shift) regions that ended up in the same post-shift window receive genuinely zero (or numerically negligible) attention weight to each other — not just that the code "looks right."
2. **Part B — fine-tuning comparison**: load pretrained `microsoft/swin-tiny-patch4-window7-224` and a comparably-sized pretrained ViT (reuse Module 9's fine-tuning setup/checkpoint choice for consistency), fine-tune both on the same CIFAR-10 subset (resized to 224×224) for the same budget, and compare: final accuracy, parameter count, peak memory (`torch.cuda.max_memory_allocated()` if CUDA available, else note CPU-only skip), and throughput (images/sec).
3. **Higher-resolution throughput comparison**: re-measure throughput for both models at a higher resolution (e.g. 384×384, resizing inputs accordingly — note Swin's window-based attention scales more gracefully than ViT's global attention as resolution grows; if either model's pretrained position embeddings don't natively support the higher resolution, either use the model's built-in interpolation support if available, or clearly document the limitation and measure what's measurable).
4. Closing markdown cell summarizing the efficiency-accuracy tradeoff observed.

- [ ] **Step 2: Derive the starter notebook**

Stub `window_partition`/`window_reverse`, `WindowAttention`'s forward pass, and the shifted-window roll/mask/reverse-roll logic, each with `raise NotImplementedError("TODO: ...")` and a hint. Keep the brute-force reference implementation, unit tests, masking-correctness check, and Part B/C fine-tuning/comparison cells identical to the solution.

- [ ] **Step 3: Smoke-test both notebooks**

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m10_solution_out.ipynb notebooks/10-swin/exercise_solution.ipynb
```
Expected: exit code 0, no cell errors, the brute-force-reference unit tests and the masking-correctness check actually assert success (not silently pass on a trivial case), and both pretrained checkpoints genuinely load real weights.

Run:
```bash
SMOKE_TEST=1 jupyter nbconvert --to notebook --execute --output /tmp/m10_starter_out.ipynb notebooks/10-swin/exercise_starter.ipynb
```
Expected: fails with `NotImplementedError`.

Verify cell `source` fields are line-lists in both files after saving.

- [ ] **Step 4: Commit**

```bash
git add notebooks/10-swin/exercise_starter.ipynb notebooks/10-swin/exercise_solution.ipynb
git commit -m "Add Module 10 coding exercise notebooks (starter + solution)"
```

---

### Task 7: Final verification and deploy

**Files:** none — verification only.

- [ ] **Step 1: Full local build**

Run: `bundle exec jekyll build` — expect no errors.

- [ ] **Step 2: Verify nav reachability for both new modules**

Run: `grep -oE 'href="[^"]*1[0]?-[a-z-]+/?"' _site/index.html | sort -u` — this pattern is imprecise for double-digit slugs, so instead run two explicit checks:
```bash
grep -o 'href="[^"]*09-vit/*"' _site/index.html | sort -u
grep -o 'href="[^"]*10-swin/*"' _site/index.html | sort -u
```
Expected: both produce at least one real nav link.

- [ ] **Step 3: Run the full automated test suite**

Run: `node --test test/*.test.mjs` — expect all pass, including `quiz-data.test.mjs` covering both new quiz files.

Run: `cd worker && npm test` — expect all pass (unaffected by this plan, confirm no regression).

- [ ] **Step 4: Verify each new module's quiz and chat widget render**

For each of the two new modules, confirm (via grep on the built HTML) the embedded quiz JSON parses to 8-10 questions and `chat-widget-root` appears on the page.

- [ ] **Step 5: Push and confirm CI**

```bash
git push origin master
gh run watch --exit-status
```
Expected: the Pages workflow succeeds. (The Worker deploy workflow will also trigger since it touches `modules/**`; its deploy step is gated to skip, not fail, when `CLOUDFLARE_API_TOKEN` isn't configured — expected, not a blocker.)

- [ ] **Step 6: Verify live**

Run, with cache-busting query params:
```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/09-vit/?cb=$(date +%s)"
curl -s -o /dev/null -w "%{http_code}\n" "https://nsteve2407.github.io/llm-transformers-course/modules/10-swin/?cb=$(date +%s)"
```
Expected: both `200`.
